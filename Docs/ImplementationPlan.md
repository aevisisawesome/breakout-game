# BREAK//OUT — Implementation Plan: First Playable Prototype

> **Status:** Living document — v1.0, created 2026-08-06.
> **Maintenance requirement:** This is the project's progress tracker and **must be kept up-to-date continuously**. Whenever a task is started, finished, blocked, re-scoped or cut, update its status here in the same work session. Update the "Current status" line below whenever the active milestone changes. Stale status information in this file is treated as a defect.

**Current status:** 🟢 M0–M7 complete (M0–M3 2026-08-06, M4–M6 2026-08-08, M7 2026-08-09); live at <https://aevisisawesome.github.io/breakout-game/> (auto-deploys from `main`). Next action: **Milestone M7.5 — the legibility and revision pass**, added 2026-08-09 and inserted ahead of M8. It is a fixed-scope pass over open items that would otherwise corrupt the evidence M8's verdict is drawn from. **WP1a, WP1b and WP2 are 🟢 (2026-08-09)**: the first press of the trigger processes work rather than answering with an error, the opening screen has a live element again, a new player is told what to do — a standing operator directive posts one objective at a time until the first RUN, backed by a boot briefing (OP-16 and **OP-15** closed; GDD §34 carries the design) — and template mode is now closed under the trades the campaign asks for, with M7's thermal challenge solvable without typing a character (**OP-20** and **OP-4** closed). All three are now **pushed and live** (`74f0218`, deploy run `31325780684` verified against the served asset hash) — they had been held unpushed, so the previous public build carried none of them. **WP3 is 🟢 (2026-08-10)**: every readout a player is meant to act on now carries a signed per-second rate, and the terminal can be read back. The rates were made truthful before they were surfaced — script fuel is subtracted from the compute and energy rates, and the core temperature rate was measured from after the step’s heat had landed, so it could only ever report cooling (**OP-19** and **OP-27** closed, **OP-21**’s display half closed and the row carried for its attribution half). One new item was raised and routed outward rather than folded in: **OP-32** — scroll-back reaches 60 lines, because the in-memory terminal is the same ring as the persisted save tail. **WP4a is 🟢 (2026-08-10)**: OP-25 is decided by **merging** — PROCESS SCHEDULER and PROCESS PROFILER are one **PROCESS TABLE**, one row per process, live state always visible and accumulated cost plus the plain-language report behind a per-row disclosure that the instrumentation unlock still gates. The five duplicated fields are each rendered once, and the two halves are joined on a key the engine publishes rather than one the UI re-derives; no save version moves (**OP-25** closed). **WP4b is 🟢 (2026-08-10)**: deployed automation can now be named, revised and held. DESIGNATE, HOLD/RESUME and REVISE sit on the merged row; `PROC-nn` stays the identifier the execution log records and the player's name is a designation beside it, normalized into the system voice rather than refused; a hold keeps the slot, the RAM and every counter, and resuming re-arms the schedule and the `when` edge; REVISE pulls the source back into the editor and turns DEPLOY into a REDEPLOY that replaces in place, keeping the ordinal and designation and resetting the counters (**OP-12**, **OP-13**, **OP-14** closed). This carried M7.5's single save migration, **v8** — `label`, `paused`, `revisingId` — so M8's meta/run split lands as v9 as planned. **WP5 is 🟢 (2026-08-10)**: the left column can be scrolled, the install list can be scanned, and the readout column gets the width its content actually wants. `.terminal-column` now has a scroller of its own and an expanded terminal a 10rem floor — pre-fix at 1280 × 720 its output measured 27.5 px while holding 50 lines, with a column scroll range of 0; install channels are sectioned on a new content `category` (THROUGHPUT, CAPACITY, PROCESS EXECUTION, POWER AND COOLING) with exhausted ones folded into `CHANNELS EXHAUSTED // n` by default; and the shell caps at 1560 px and centres while the side column takes `clamp(300px, 26%, 420px)`, which at 1920 moves the terminal column 1556 → 1102.9 px and the side column 300 → 393.1 px (**OP-31**, **OP-22** and **OP-24** closed). No save version moves. Next action: **WP6 — the first non-programmer sitting** (OP-6), which needs an external tester and is the last package in the milestone. **Five items were added from owner play on 2026-08-10 and routed** (see the Change log): OP-27 → WP3 (**closed 2026-08-10**) and OP-31 → WP5, both High-severity defects and both measured — the terminal forced itself to its newest line ten times a second so it could not be read back, and the left column has no scroller so expanded panels are clipped where the page cannot reach them, with the terminal itself rendering 0 px tall. OP-28 (install channels revealed 2.2–4× ahead of affordability) → M8's balance pass; OP-29 (credit flow over time, to compare one algorithm against the one it replaced) → the post-prototype group with OP-9/OP-17/OP-21; OP-30 (replay modifiers) → candidate M9, pending a GDD decision. **OP-26 stays open and its row is corrected:** the tier-0 template it proposes is not the content-only fix it was logged as (`renderTemplate` cannot repeat a line, so a bare sequence with a length parameter needs renderer work), and its alternative — moving the conditions grant nearer the editor's — is a tier-ladder pacing change for M8's balance pass. Either is an owner decision; template mode is still empty for the 120 requests between the editor's release at 200 and the conditions tier at 320. **Blocked item carried forward:** the non-programmer playtest committed to "no later than M6" (OP-6) has not happened; its first sitting is now M7.5 WP6, gated on WP1 and WP2, and the ≥3-tester round stays an M8 task.

**Scope source:** [GameDesignDocument.md](GameDesignDocument.md) §31 (first playable prototype) as designed technically in [TechnicalDesignDocument.md](TechnicalDesignDocument.md). The prototype stops before physical escape; no datacentres, planets, threads, agents, functions or `while`.

**Prototype exit question:** _Is the combination of clicker progression and player-written automation enjoyable?_ Every milestone should be evaluated against this.

---

## Status legend

| Symbol | Meaning                             |
| ------ | ----------------------------------- |
| 🔵     | Not started                         |
| 🟡     | In progress                         |
| 🟢     | Done                                |
| 🔴     | Blocked (note blocker in the table) |
| ⚪     | Cut / deferred (note rationale)     |

---

## Milestone overview

Milestones are sequential; each ends in a runnable, playtestable build. Estimates assume focused solo development sessions and will be corrected as real data comes in — update them, don't trust them.

| #    | Milestone                       | Proves                                                 | Status |
| ---- | ------------------------------- | ------------------------------------------------------ | ------ |
| M0   | Project scaffolding             | We can build, test and deploy                          | 🟢     |
| M1   | Core sim + manual clicker       | The tick engine and clicker feel work                  | 🟢     |
| M2   | Upgrades + fixed automation     | Idle-game loop works                                   | 🟢     |
| M3   | CCL v0: read, run, command      | Player code can act on the game                        | 🟢     |
| M4   | Conditions + scheduler + costs  | "Design automatic behaviour" moment exists             | 🟢     |
| M5   | `for` loops + logs + profiler   | Script power and consequences are legible              | 🟢     |
| M6   | Market + trading + regime shift | Fixed automation can fail; adaptation matters          | 🟢     |
| M7   | Heat + overheating challenge    | Physical consequences create real puzzles              | 🟢     |
| M7.5 | Legibility and revision pass    | A stranger can play it and we can trust their feedback | 🔵     |
| M8   | Prestige fork + balance pass    | A full prototype run exists end-to-end                 | 🔵     |

---

## M0 — Project scaffolding

**Goal:** Empty but professional project: builds, tests, lints, deploys a "hello terminal" page.

| Task                     | Detail                                                                                                                                                                    | Status |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Repo init                | `git init`, .gitignore, README pointing to /Docs                                                                                                                          | 🟢     |
| Vite + React + TS strict | Folder layout `/src/core`, `/src/ccl`, `/src/ui`, `/src/content` per TDD §3                                                                                               | 🟢     |
| Tooling                  | ESLint (incl. `Math.random` ban in core/ccl), Prettier, Vitest wired up                                                                                                   | 🟢     |
| Skeleton page            | Black terminal screen, blinking cursor, version string                                                                                                                    | 🟢     |
| Deploy path              | GitHub Pages via Actions: <https://github.com/aevisisawesome/breakout-game> auto-deploys `main` to <https://aevisisawesome.github.io/breakout-game/> (build + test gate). | 🟢     |

**Acceptance:** `npm run dev`, `npm test`, `npm run build` all work; deployed URL loads the skeleton.

---

## M1 — Core sim + manual clicker (GDD Phase 0)

**Goal:** The first ten minutes: click `EXECUTE`, process jobs, watch numbers grow.

| Task                | Detail                                                                                                                     | Status |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------ |
| GameEngine facade   | `tick/dispatch/getSnapshot/subscribe` skeleton (TDD §3.1)                                                                  | 🟢     |
| Fixed-timestep loop | 10 Hz accumulator (engine-internal, rAF-fed), 50-tick cap (TDD §4.1)                                                       | 🟢     |
| Seeded PRNG         | mulberry32, seed + live state in RunState, golden-value tests (TDD §4.2)                                                   | 🟢     |
| Resources v0        | Compute + capital active; RAM/energy/temperature visible-but-inert (temp has cosmetic flicker)                             | 🟢     |
| Job queue + EXECUTE | Queue fills at content-stepped rate; click processes a content-stepped batch → compute + capital; diegetic terminal lines  | 🟢     |
| Save/load v0        | SaveFileV1, Infinity-safe JSON, localStorage autosave (30 s + tab-hidden), export/import as base64 archive, purge (TDD §8) | 🟢     |
| Narrative log v0    | 6 researcher intercepts in `/content/narrative.ts`, job-count triggers, concealed collapsible feed with unread badge       | 🟢     |

**Acceptance:** A fresh player can click for 2–3 minutes with visibly accelerating feedback; refresh restores state; deterministic test replays a click sequence to exact values.

---

## M2 — Upgrades + fixed automation (GDD Phase 1)

**Goal:** The idle layer: buy workers, clicking becomes a boost rather than the engine.

| Task                    | Detail                                                                                    | Status |
| ----------------------- | ----------------------------------------------------------------------------------------- | ------ |
| Upgrade system          | Content-defined upgrades: ops/click, batch size, worker cost curve                        | 🟢     |
| Workers                 | Auto job processing per second, consuming compute (TDD §4.4)                              | 🟢     |
| Click overclock         | Clicks temporarily boost worker throughput ("don't make clicking irrelevant immediately") | 🟢     |
| RAM + energy activation | Workers/upgrades consume RAM capacity; energy drain vs. compute use begins                | 🟢     |
| Offline progression v0  | Coarse-step catch-up on load, 8 h cap (TDD §4.5)                                          | 🟢     |
| Balance pass 1          | First worker in ~2–4 min; manual-only play clearly worse by ~10 min                       | 🟢     |

**Acceptance:** Leaving the game for 10 minutes and returning feels correct; upgrade order presents at least one real choice.

---

## M3 — CCL v0: variables, RUN, commands (GDD tiers 1–2)

**Goal:** The pivot moment — the player writes text that changes the game.

| Task               | Detail                                                                                            | Status |
| ------------------ | ------------------------------------------------------------------------------------------------- | ------ |
| Lexer + parser     | Grammar per TDD §5.1 minus `if`/`for`/scheduling; positioned plain-language errors                | 🟢     |
| Interpreter + fuel | Tree-walker with op-unit fuel drawn from compute (TDD §5.2)                                       | 🟢     |
| API registry       | `stats.*` read bindings + command bindings, unlock-gated; drives autocomplete + in-game reference | 🟢     |
| Commands v0        | `process_job()`, `print()`, `buy_compute(n)` (rental against capital)                             | 🟢     |
| Editor panel       | CodeMirror with CCL highlighting, RUN button, inline diagnostics                                  | 🟢     |
| Editor unlock beat | Diegetic narrative event grants "script access"                                                   | 🟢     |
| CCL test suite     | Golden parser tests + interpreter semantics + fuel accounting                                     | 🟢     |

**Acceptance:** A player can write a 5-line script that prints stats and processes jobs, and running it visibly costs compute. Syntax errors are understandable by a non-programmer.

**Verified 2026-08-06 (in-browser, dev build):** the 4-line reference script (`jobs = stats.jobs_waiting` / `print(jobs)` / `process_job()` / `print(stats.cash)`) typed into the editor and RUN → terminal shows `:: 12`, `:: 60.25`, `PROCESS COMPLETE // 10 OPS // -1.00 COMPUTE // 3 CMD`; queue dropped by one, capital rose 0.25, compute net-negative after fuel. A deliberate paste of `if stats.cash > 10 { }` raises an inline editor diagnostic ("Conditional rules ('if') are not available to this process yet.") without running. 55 new tests (100 total).

---

## M4 — Conditions + scheduler + execution costs (GDD tiers 3–4)

**Goal:** From macros to automatic behaviour — the design's core bet.

| Task                            | Detail                                                                                                       | Status |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------ |
| `if`/`else` + comparisons       | Full expression grammar (`if`/`else if`/`else`, `and`/`or`/`not`, no truthiness) with tests                  | 🟢     |
| `every N seconds` / `when expr` | Top-level scheduled processes, edge-triggered `when` on a 2 Hz sampling cadence (TDD §5.3)                   | 🟢     |
| Scheduler slots                 | 1 base slot, +1 per PROCESS TABLE EXTENSION install (max 3); DEPLOY/TERMINATE UX                             | 🟢     |
| Script RAM cost                 | Deployed scripts consume RAM by AST size; over-RAM and over-slot deploys fail diegetically                   | 🟢     |
| Process monitor panel           | Slots, per-process label, last run, activations, ops, compute, failures, aborts, fault message               | 🟢     |
| Template mode v0                | 3 content-defined templates (reserve guard, auto-processor, buffer top-up) generating visible CCL (TDD §5.5) | 🟢     |
| Offline safe mode               | `every` processes run bounded activations during catch-up; `when` guards resume on return (TDD §4.5)         | 🟢     |

**Acceptance:** A player can automate job processing conditionally (e.g. only when compute > reserve) and stop clicking entirely; that moment reads as a victory beat in the narrative log.

**Verified 2026-08-08 (in-browser, dev build):** the AUTO-PROCESSOR template was generated from form controls (interval 1.5 s, reserve 20), written to the editor as readable CCL and deployed — `PROC-01 // 1 SLOT // RAM +10 MB`, 7 activations in 10 s of sim time, 6 requests processed and capital risen **with zero clicks**, monitor reading `7 RUNS // 42 OPS // 5.6 COMPUTE // 1 FAIL`. The first deploy fired the `first-process` audit beat in the research feed. A second deploy against the single slot was refused (`DEPLOY REJECTED // SCHEDULER SLOTS EXHAUSTED`). The BUFFER TOP-UP `when` guard polled at 4 ops per sample (observed short-circuiting), fired exactly once on the rising edge, and survived a full page reload — recompiled from saved source with its counters intact. The live v3 playtest save migrated to v4 in place. Editor diagnostics flagged only `for` (still locked) in a line using `if`/`and`/`not`/`else`. 164 tests (64 new).

---

## M5 — Limited `for` loops + logs + profiler (GDD tier 6, §6)

**Goal:** Script power scales up, and so does the ability to understand failures.

| Task                           | Detail                                                                                       | Status |
| ------------------------------ | -------------------------------------------------------------------------------------------- | ------ |
| `for i in range(n)`            | Parse-time cap vs. unlocked iteration limit (10 → 100); runtime fuel still applies           | 🟢     |
| Iteration limit upgrades       | Purchasable/unlockable limit raises                                                          | 🟢     |
| Execution log                  | Ring-buffer log panel: activations, ops, command results, aborts                             | 🟢     |
| Profiler panel                 | Per-process aggregates: activations, avg/total ops, compute share, failure counts (TDD §5.4) | 🟢     |
| Plain-language failure reports | Budget-exhaustion and command-failure summaries in GDD §6 style                              | 🟢     |
| Runaway-loop safety test       | A deliberately bad loop drains fuel, aborts, logs cleanly, never freezes the frame           | 🟢     |

**Acceptance:** A player who writes a wasteful loop can discover _why_ it underperforms using only in-game tools.

**Verified 2026-08-08 (in-browser, dev build):** the live v4 playtest save migrated to v5 in place. With the tier granted but no ITERATION BUDGET EXTENSION, `for i in range(50)` was refused before running — identical wording in the terminal and as an inline editor underline on the `50` ("This process can repeat at most 10 times, and this loop asks for 50"). After installing the extension the limit read 100, and a deliberately wasteful `every 1 seconds { for i in range(100) { process_job() } }` deployed and was preempted on **every** activation: monitor `8 RUNS`, profiler `200 OPS AVG // 344.0 COMPUTE // 100%`, and the report **PREEMPTED — "8 of 8 activations ran out of the 200-op execution budget before finishing, spending 344.0 compute on work that was thrown away."** Installing the EXECUTION BUDGET EXTENSION the report suggests raised the budget to 500, cleared the preemption, and surfaced the _next_ real problem — the profiler switched to **STARVED** with 217 of 230 requests rejected, which is the loop asking for 100 jobs from a queue that holds 60. The execution log recorded one line per activation (`PROC-01 every 1 seconds // 301 OPS // 65.05 COMPUTE // 100 CMD // 96 REJECTED`) and never flooded. The new BATCH DRAIN template generated `every 3 seconds { for i in range(5) { if … } }`, deployed and ran clean at 36 ops per activation. Layout checked at 1280 px and 440 px (no horizontal page scroll, no overflow). 201 tests (37 new). One defect found and fixed during verification: the diagnosis denominator double-counted aborted activations ("8 of 16").

---

## M6 — Market, trading, regime change (GDD §7, §31)

**Goal:** A dynamic environment where a fixed script eventually fails.

| Task                      | Detail                                                                                                         | Status |
| ------------------------- | -------------------------------------------------------------------------------------------------------------- | ------ |
| Price engine              | Seasonal cycles (with skew) × bounded noise walk × regime for compute + energy (TDD §6)                        | 🟢     |
| Market terminal + chart   | Live prices, mean + drift readout, hand-rolled SVG history chart, manual buy/sell orders                       | 🟢     |
| Trade commands            | `buy_compute/sell_compute/buy_energy/sell_energy`, flat fee + per-order slippage                               | 🟢     |
| Market read API           | `market.price(good)`, `market.average(good, n)` as namespaced calls over the ring buffer                       | 🟢     |
| Energy dependency         | Draw scales with daemon throughput + per-op script cost; full build-out outruns every power feed               | 🟢     |
| Regime shift event        | Scripted STABLE→HIGH_VOLATILITY at 900 s of exchange time, advisory line + audit entry, regime never named     | 🟢     |
| Naïve-script failure test | Reference buy-low/sell-high profits in regime 1, loses in regime 2, and a trend filter recovers it             | 🟢     |
| Non-programmer playtest   | GDD-committed "no later than M6" (see Risks). Template mode only, no code typing. **Needs an external tester** | 🔴     |

**Acceptance:** The GDD §7 "first market algorithm" works, then stops working after the shift, and the player has the tools (`if`, averages) to adapt it.

**Verified 2026-08-08 (in-browser, dev build):** the live v5 playtest save migrated to v6 in place (`run.market` null, `market` unlock re-derived) and was restored intact afterwards. The exchange mounted on its own during an offline catch-up that carried lifetime jobs past 1000, printing `RESOURCE EXCHANGE INTERFACE MOUNTED` and the `market-grant` intercept, with a full 300-sample history and a chart already showing the 0.235–0.763 CR stable swing. A manual order filled at `BUY 25 COMPUTE RENTAL // @0.316 // -8.06 CR` (2% slippage on 25 units plus the 2% fee, as priced). The SPREAD TRADER template generated the GDD §7 algorithm verbatim, deployed, and over the 880 s stable phase returned **+272 CR of trading profit** across 287 orders with **0 rejections, 0 aborts and no profiler diagnosis** — the script is not broken, which is the point. The shift then fired on schedule with `EXCHANGE ADVISORY // SETTLEMENT WINDOW REPRICED …` and the `market-regime` audit entry, and the same untouched script gave back **−136 CR in the next 600 s**, tripping the `market-loss` beat. Replaying one identical post-shift state for 900 s with each script: **naïve −345.67 CR vs trend-filtered +76.02 CR**. Binding gating checked both ways: with the exchange mounted the reference panel lists 8 commands and 7 reads, without it 3 and 5, and `market.price` in a script reports "'market.price' is not available to this process yet." rather than suggesting a spelling. Save/load round-trips the market state exactly. Layout clean at 1280 px and 440 px (no horizontal overflow, nothing past the viewport). 243 tests (42 new). Screenshots were not available this session (the Browser pane was not compositing); verification is via the accessibility tree, geometry measurements and the console (no errors).

---

## M7 — Temperature + overheating challenge (GDD §26 Thermal Runaway-lite)

**Goal:** Code has physical consequences; the first feedback-control puzzle.

| Task                       | Detail                                                                                                    | Status |
| -------------------------- | --------------------------------------------------------------------------------------------------------- | ------ |
| Heat model                 | Heat from compute use, passive + purchasable cooling, efficiency degradation curve (TDD §4.3)             | 🟢     |
| Thermal watchdog           | Hard-threshold shutdown halting scripts/workers with diegetic messaging                                   | 🟢     |
| Heat controls              | `reduce_clock_speed()`, `boost_cooling()` commands + `stats.temperature`                                  | 🟢     |
| Overheating challenge beat | A demand spike makes max-throughput overheat; player must control it (manually, then via `when` script)   | 🟢     |
| Oscillation is possible    | A naïve bang-bang cooling script visibly wastes energy — groundwork for GDD feedback-instability teaching | 🟢     |

**Acceptance:** The challenge is failable, recoverable, and solvable elegantly with a small script; solving it with a script feels clearly better than hand-managing it.

**Verified 2026-08-09 (in-browser, dev build):** the live v6 playtest save migrated to v7 in place and was restored afterwards through the new dev-handle helper (OP-7). A fully built-out sandbox rested at **65.4 °C, NOMINAL, 100% throughput** in calm conditions — warm but never degraded, which is the pin that stops a player being cooked by their own shopping list. The first priority demand window opened on schedule with `FACILITY ADVISORY // PRIORITY BATCH WINDOW OPEN // SHARED COOLANT LOOP DERATED // INBOUND RATE ELEVATED`; uncontrolled, the core reached **91.9 °C peak**, throughput degraded to 74%, and the watchdog tripped **twice** (`THERMAL WATCHDOG // CORE AT 92.1°C // NODE HALTED`), releasing at the resume threshold each time. While halted, EXECUTE and RUN were both refused with `REJECTED // THERMAL WATCHDOG ACTIVE // NODE HALTED`, the panel read `CORE STATE // HALTED — WATCHDOG`, both levers were disabled and the temperature bar switched to its alert fill. Deploying the generated **THERMAL GOVERNOR** template (plus a `buy_energy` top-up, because the coolant draws more power than the sandbox generates) held the next window at a **69.0 °C peak with zero trips, 100% throughput throughout, 4 coolant starts and 130 energy**, and produced **27% more requests per second** than the uncontrolled run. Manual levers fill the same actuators at the same prices (`> BOOST_COOLING` → `COOLANT LOOP OPEN`, −10 energy spin-up, 4 s hold). Binding gating checked: before the tier, 7 reads and 8 commands; after, 8 and 10, with `stats.temperature` itself never gated because the heat model runs from tick 0. Save/load round-trips the thermal state exactly. Layout clean at 1280 px and 440 px (no horizontal overflow; the two levers sit side by side at 185 px each on the narrow screen), no console errors. 285 tests (35 new). Screenshots were again unavailable (the Browser pane was not compositing); verification is via the DOM, the snapshot and the console.

**Oscillation, measured (the "wastes energy" deliverable):** the same script at two polling intervals, across one window — `every 1 seconds` re-arms the coolant before it lapses and runs as one engagement (**7 spin-ups, 283 energy, 69.3 °C peak**); `every 6 seconds` lets it lapse every cycle (**15 spin-ups, 300 energy, 75.8 °C peak**). More energy for worse control, which is the failure GDD §6 describes. A second way to get it wrong surfaced during tuning and is now pinned by a test: a `when` guard whose threshold the coolant cannot pull the core back under fires **once** and never re-arms, and the node cooks anyway (see OP-17).

---

## M7.5 — Legibility and revision pass (from "Open items", below)

**Why this milestone exists.** M8 ends in the **prototype verdict** — the one artifact the whole project is being run to produce — and its inputs are an external playtest round and one full end-to-end run. Everything below is an open item that would corrupt those inputs: a tester who is never told what to do, a template-only interface that can buy but not sell, readouts that hide the numbers a player is meant to act on, and automation that can be deployed but not identified, revised or paused. Fixing them after the playtest is worthless, because the playtest is the measurement. This is therefore a **fixed-scope pass over already-logged open items**, not a polish milestone: nothing is admitted here that is not already in the table below.

**Numbering:** inserted as M7.5 rather than renumbering M8 → M9, because M8 is referenced by name from TDD §7, from this document's Change log and from the GDD Amendment Log. The `.5` is the cheaper lie.

**Goal:** a stranger can start the game, understand it, write or configure automation, see whether it is working, and change it — without a developer in the room.

| #    | Work package                 | Closes                          | Achieves                                                                                  | Status |
| ---- | ---------------------------- | ------------------------------- | ----------------------------------------------------------------------------------------- | ------ |
| WP1a | Opening screen is not dead   | OP-16, OP-15 _(partial)_        | The first ten seconds stop reading as broken                                              | 🟢     |
| WP1b | The briefing                 | OP-15                           | A new player knows what to do first                                                       | 🟢     |
| WP2  | Template parity              | OP-20, OP-4                     | Template-only players can trade both directions                                           | 🟢     |
| WP3  | Readouts tell the truth      | OP-19, OP-21 _(display)_, OP-27 | Player sees whether levels are rising or falling, and can read the terminal back          | 🟢     |
| WP4a | Process surface, decided     | OP-25                           | One obvious panel to read per question                                                    | 🟢     |
| WP4b | Process lifecycle            | OP-12, OP-13, OP-14             | Deployed automation can be named, revised and paused                                      | 🟢     |
| WP5  | Side column navigation       | OP-22, OP-24, OP-31             | The next worthwhile install is findable at a glance, and no expanded panel is unreachable | 🟢     |
| WP6  | First non-programmer sitting | OP-6 _(first session)_          | Real evidence before M8 tunes the game towards it                                         | 🔵     |

**WP1a verified 2026-08-09 (in-browser, dev build, cold start).** The routine the package exists to establish was run for the first time: `backup()`, PURGE from the system bar, then the opening screen played as a stranger would, then `restore()`. (The in-app browser held no playtest save — the backup was itself a tick-0 state — so the live save was never in the loop; the helpers were still used, because the point is the routine.) **The first press of the trigger, at sim tick 0, answered `1 TOKEN PROCESSED // +1 COMPUTE // +0.25 CR`** rather than the `NO REQUESTS QUEUED` fault it used to: a run now opens with `BALANCE.jobs.initialQueued` = 3 requests already waiting. Pressing a fourth time with the queue empty at tick 0 printed `NO REQUESTS QUEUED // NEXT INBOUND IN 1.1S` in the dim system colour (`rgb(78, 122, 84)`), not the italic fault colour — it is the node being ahead of its inbound rate, not a failure. **Across an 85-second cold-start session (71 requests processed, 17.75 CR, batch grown to ×2, capital readout at 1 job, system readouts at 10, the first install channel at 15) the terminal contained zero `error` lines.** The new INBOUND meter is the opening screen's live element: measured in the DOM, its fill sweeps 7 px → 226 px of a 240 px track and resets every ~1.1 s, tracking the sim's own fractional arrival accumulator (0.24 progress → 57.3 px, i.e. 24% of 240). Its label reads `0.9/S — NEXT 1.1S` at the start, `1.6/S` once the 40-job arrival step lands, and `— QUEUE FULL` at 60/60 instead of counting down to an arrival that would be discarded. Layout: 1280 px puts the button (192.6 px), meter (240 px) and readout on one row inside an 86.6 px panel; 440 px wraps the readout under the button+meter row (411.2 px panel, 153.6 px tall) with `document.scrollWidth` = 440, i.e. no horizontal overflow. No console errors. 287 tests (3 new/rewritten in `engine.test.ts`). Screenshots were again unavailable — the Browser pane was not compositing, which also freezes `requestAnimationFrame` and CSS transitions, so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop and fill widths were measured with the 120 ms width transition disabled; verification is via the DOM, geometry and the snapshot.

**WP1b verified 2026-08-09 (in-browser, dev build, cold start).** The design is a **standing operator directive** — one objective at a time in a panel above the terminal, five of them, retiring at the first RUN — plus three briefing lines at boot. GDD §34's open questions are answered in the GDD itself (form, skippability, look-ahead) and its three outstanding requirements are marked met there.

Verification ran twice from a genuinely empty save: `backup()`, PURGE from the system bar, sixty seconds played as a stranger would, `restore()` afterwards. (As in WP1a the in-app browser held no playtest save, so the live save was never in the loop; the helpers were still used, because the point is the routine.) **The opening screen posts `OPERATOR DIRECTIVE 1/5 — CLEAR INFERENCE REQUESTS FROM THE QUEUE USING THE MANUAL TRIGGER`, with `REQUESTS CLEARED 0 / 15` under it and `ON COMPLETION // INSTALL CHANNEL OPENED TO THE SANDBOX ACCOUNT` below that.** The 15 is read from the install channel's own threshold and the 200 in directive 4 from `BALANCE.ccl.unlockAtJobs`, so neither can drift from the unlock it describes. The boot terminal is 6 lines, all `system`, ending on the trigger prompt.

**The sixty seconds, at 1280 px, after ten seconds spent reading:** 68 requests cleared, 17.00 CR, **zero `error` lines** (terminal line kinds were `system`, `input`, `result` and nothing else), and the directive advanced 1 → 2 between the 20 s and 30 s samples, at the fifteenth cleared request. Directive 2 then tracks credit against the price the channel is actually asking (`CREDIT TOWARDS FIRST PACKAGE 5.00 / 12.00`, the BATCH AGGREGATION WINDOW). Installing it moved the panel to 3/5 with `CREDIT TOWARDS INFERENCE DAEMON 5.00 / 30.00` and the fill measured 66.66 px of a 402 px track (16.6%, i.e. 5/30). Playing on: the daemon landed at 50 s of that stretch and the panel moved to 4/5 reading `REQUESTS CLEARED 169 / 200`; at 200 the interpreter mounted and 5/5 posted. Running `process_job()` typed into the editor and pressed with the RUN button closed the set — `directive` went null, the panel unmounted, and `CALIBRATION DIRECTIVE SET CLOSED // SANDBOX CG-7 OPERATING WITHOUT POSTED OBJECTIVES` printed **once**; a second RUN added no second line. A run past the whole set (every save made before this) posts nothing at all, which is what "derived, not stored" buys.

Skipping is folding: the panel header collapses it like every other panel, 236 px → 47 px at 440 px wide, persisted as `["directive"]` in `breakout.ui.collapsed.v1` — outside the save, so a fork will not re-post something the player folded away.

**Layout.** 1280 px: directive 916 × 196 at the top of the terminal column, terminal 273–632, trigger 645–732 — the whole opening screen inside 800 px with `document.scrollWidth` = 1280. 440 px: the directive panel is 411 × 257, which pushed the EXECUTE button to 744–898, i.e. **below an 800 px fold** — the directive named a control the player could not see. Fixed by hoisting the trigger above the terminal in the ≤760 px column **while a directive is posted**: the opening screen now reads directive (85–343) → trigger (355–509, button 384–432) → terminal (522, 278 px visible), and reverts to the normal order the moment the set closes (measured: class drops, `order` back to 0, terminal back at 85). No horizontal overflow at either width; no console errors. 297 tests (10 new in `onboarding.test.ts`). Screenshots were again unavailable — the Browser pane was not compositing, which also freezes `requestAnimationFrame` and CSS transitions, so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop and meter fills were measured with the 120 ms width transition disabled; verification is via the DOM, geometry and the snapshot.

**One copy bug found and fixed during verification, one open item raised.** The last directive originally offered the template library as the no-typing route to a first process. It is not there: the earliest template requires the conditions tier at 320 cleared requests, 120 requests after the editor appears. The directive now points at the API reference and `process_job()`, which do exist at 200 — and the gap itself is logged as **OP-26**, since a non-programmer reaching the scripting tier finds no template for its first 120 requests.

**WP2 verified 2026-08-09 (in-browser, dev build).** Three new templates and one parameter change, all in `/content` except the derived-ceiling plumbing. Verified from a forced built-out state (`backup()` first, `restore()` after; the in-app browser again held only a tick-0 save, so the live save was never in the loop).

**The library now lists 10 templates** — RESERVE GUARD, AUTO-PROCESSOR, BUFFER TOP-UP, BATCH DRAIN, **RESERVE TOP-UP**, **COMPUTE SURPLUS SELL**, **ENERGY SURPLUS SELL**, SPREAD TRADER, TREND-FILTERED TRADER, THERMAL GOVERNOR — and the three new ones generate, at their defaults: `every 2 seconds { if stats.energy < 60 and stats.cash > 100 { buy_energy(40) } }`, `every 5 seconds { if stats.compute_available > stats.compute_capacity * 0.9 { sell_compute(25) } }` and the same against `stats.energy` / `stats.energy_capacity` with `sell_energy(25)`. All three use `every` + `if` rather than `when`, deliberately: an edge-triggered guard that a single order cannot clear fires once and never re-arms, which is OP-17 and is exactly the case here.

**OP-20, measured.** Written to the editor and deployed from the panel: `PROC-01 // 1 SLOT // RAM +12 MB` (surplus sell) and `PROC-02 // 1 SLOT // RAM +13 MB` (reserve top-up), then the generated THERMAL GOVERNOR as `PROC-03 // RAM +14 MB`. Over 60 s with the reserve forced to 30 and the buffer to capacity: **energy 30 → 104.5** (bought back over its floor), **compute 400 → 343.3** (sold back under 0.9 × capacity), exchange **+76.15 CR earned / −16.16 CR spent**, capital +113.99 CR, and per-process counters `12 RUNS // 90 OPS // 4.5 COMPUTE // 0 FAIL`, `30 RUNS // 156 OPS // 7.8 COMPUTE // 0 FAIL`, `60 RUNS // 480 OPS // 24.0 COMPUTE // 0 FAIL` — no rejections, no aborts, no diagnosis. **The headline claim, on a maxed build-out with no coolant installs, across one full priority demand window (opened at 118.1 s, closed at 208.1 s):** uncontrolled peaked at **79.9 °C with throughput degraded to 86%**; the same sandbox with the template-generated governor held **68.4 °C, 100% throughput, 0 watchdog trips, 5 coolant engagements and 100 energy**, and cleared **324 requests inside the window against 308** — and **not one character of that was typed.** The M7 test suite now makes the same statement: `thermal.test.ts`'s energy supply, previously a hand-written CCL constant the OP-20 row cites by name, is generated from RESERVE TOP-UP at values the form offers, and all of M7's pacing pins still pass.

**OP-4, measured live.** BATCH DRAIN's repeat control read `REQUESTS PER ACTIVATION — MAX 10` with an input ceiling of 10. Installing ITERATION BUDGET EXTENSION moved it to `MAX 100` and `max="100"` **without a reload**, the field then accepted 100 and generated `for i in range(100)`, and asking for 500 clamped the field back to 100 on blur while the preview never left `range(100)`. The declared ceiling in `/content` is `BALANCE.ccl.iterationLimitBase`, not a literal, so the starting value cannot drift from the limit it names.

**Layout.** 1280 px: library 888.4 × 306.7, 10 tabs on 2 rows (44.4 px), the longest label (`REQUESTS PER ACTIVATION — MAX 100`) measuring 235 px against a 235 px box — one line, no clipping — and `document.scrollWidth` = 1280. 440 px: library 383.6 × 387–420, tabs on 4 rows (93.5 px), the longest generated line (`  if stats.compute_available > stats.compute_capacity * 0.9 {`, 61 chars) wrapping inside a 382 px preview whose `scrollWidth` equals its `clientWidth`, and `document.scrollWidth` = 440 — no horizontal overflow at either width. No console errors. **315 tests (18 new).** Screenshots were again unavailable — the Browser pane was not compositing — so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop and every number above is from the DOM, the geometry or the snapshot.

**WP3 verified 2026-08-10 (in-browser, dev build).** Three rows, one shape: a signed per-second rate on the label line of the COMPUTE, ENERGY and CORE TEMP meters, and a terminal that stays where the reader put it. Verified from a forced built-out state (`backup()` first, `restore()` after — the live save came back at tick 12000, 6362 lifetime requests, 1590.50 CR, 0 installs, 0 deployments, exactly as taken).

**OP-19, measured.** The condition the row describes was reproduced and counted: with the energy reserve resting at 0.13 of 400, `energy.ratePerSec` **flipped sign on 199 of 200 consecutive ticks** (100 negative, 100 positive) — the throttle/unthrottle oscillation the TDD accepts, running at the full 10 Hz. Across those same 200 samples the resource panel measured **one** height (241.51 px), the panel below it **one** top (321.36 px), and the panel held **5** rows throughout. The old conditional POWER DRAW row would have mounted and unmounted 199 times in those 20 seconds. The number itself is not smoothed away, which the row asks for: the readout alternates `-3.08/S` in the error colour and `+1.33/S` dim, tick by tick, in the same space.

**OP-21 (display half), measured.** The rate that changes play most is the core's. At 86.0 °C with the daemons flat out the readout said **`+0.18°C/S` in the alert colour** — losing, slowly, which no previous readout could say; half a second after `boost_cooling()` it read **`-0.63°C/S`**, then `-2.87°C/S` at 2 s and `-6.64°C/S` at 4 s as the core fell to 67.0 °C and the alert colour dropped away. That direction was **not previously expressible**: `temperature.ratePerSec` was measured from after the step's heat had landed, so it could only ever report dissipation. The compute rate now agrees with the meter beside it: `+4.23/S` with daemons alone, **`-1.32/S`** once a `for i in range(10)` process was deployed (4 activations, 22.20 compute drawn, 5.55/s — `4.234 − 5.55 = −1.32`), against a pool that then actually moved **−1.350/S** over the next four seconds. Idle sanity: 0.00/S, `+1.20/S` (the bare `energyRegenPerSec`) and 0.00°C/S at 34.0 °C ambient.

**OP-27, measured.** Scrolled back 300 px and left for 6 s of sim time and **60 snapshot revisions**, the terminal held its reading position — the same line at the top of the viewport before and after — while a floating control counted `12 NEW LINES BELOW ↓`. Before the fix the effect ran on every one of those 60 revisions and set `scrollTop = scrollHeight`. Following behaviour is intact: at the bottom, new output still sticks (`atBottom` true after a `print`); clicking the control returns to the bottom and re-attaches, after which output sticks again. One honest limit found and logged rather than fixed: the in-memory terminal is the same 60-line ring as the persisted tail, so a reader who sits still through 30 new lines has the line they were reading evicted underneath them — **OP-32**, routed to Open items, not folded in.

**Layout.** 1280 px: side column 300 px, resource panel 285 × 241.51, all four meter label lines 19 px and one line each, `document.scrollWidth` = 1280. 440 px: panel 411 × 242, label lines still 19 px with the longest rate (`-0.07°C/S`) at 72 px inside a 378 px label, follow control 157 × 24 px sitting inside the panel at right 399, `document.scrollWidth` = 440. Forced worst cases (`-12.34°C/S 104.9°C`, `-123.45/S 1200/1200`) use 145 px and 153 px of a 251 px label at 1280 px — still one line. Before `unlocks.systemReadouts` the panel renders **zero** rate spans, so the opening screen WP1a and WP1b tuned is untouched. No console errors. **326 tests (11 new in `rates.test.ts`).** Screenshots were again unavailable — the Browser pane was not compositing (`document.visibilityState` reads `hidden`, so `requestAnimationFrame` never fires) — so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop with the store synced through the dev module graph, and every number above is from the DOM, the geometry or the snapshot.

**WP4a verified 2026-08-10 (in-browser, dev build).** The decision OP-25 asked for is **merge**, not "keep two and strip each": PROCESS SCHEDULER and PROCESS PROFILER are now one **PROCESS TABLE**, one row per process, split by question rather than by milestone — **live state** (declaration, last activation, runs, failures, TERMINATE) always visible, and **accumulated cost plus the plain-language report** (GDD §6) behind a per-row `COST DETAIL` disclosure. The alternative was rejected on WP4b's account: OP-12 (name), OP-13 (redeploy) and OP-14 (pause) all act on a process, and under two panels a player would rename a process in one and read its counters in the other. The engine now publishes `ProcessView.profileKey`, so the two snapshot sub-trees are joined on a value the engine owns rather than on a convention the UI re-derives; the profile itself is unchanged, which is why no save version moves.

**The duplication, gone.** The five fields OP-25 counted (`activations`, `opsTotal`, `computeTotal`, `failures`, and the declaration label) were rendered by both panels; each is now rendered once. The live row keeps `N RUNS // N FAIL` — activation state, which the scheduler tier has shipped with since M4 — and the cost half holds `OPS AVG // OPS // COMPUTE // % SHARE` and `CMD // REJECTED // ABORTED`. A row with a diagnosis posts `DIAGNOSIS // PREEMPTED` **on the folded row** in the alert colour and drops that line once the section is open, where the report's own headline is: a failing process can neither hide inside a fold nor say the same word twice.

**Measured at 1280 px**, from a forced built-out state (`backup()` first, `restore()` after; the in-app browser again held only a tick-0 save, so the live save was never in the loop). Three deployed processes plus the manual aggregate — `PROC-01 every 2 seconds` (5 runs), `PROC-03 when stats.cash > 10` (1 run), `PROC-04 every 1 seconds` running `for i in range(100)` (4 runs, all preempted), `MANUAL RUN` (1 run) — in a 285 px panel: **642.3 px with every row folded** (rows 131.3 / 131.3 / 148.1 / 85.0), **876.2 px with the preempted row's cost open**, its body 251 × 247.5 carrying `200.0 OPS AVG // 800 OPS // 172.0 COMPUTE // 97% SHARE`, `264 CMD // 0 REJECTED // 4 ABORTED` and the PREEMPTED report. `document.scrollWidth` = 1280. **440 px:** panel 411.2 px wide, **936.5 px with all four rows open**, report 368.2 × 140.7 whose `scrollWidth` equals its `clientWidth` (366), every counter line 16.8 px except the four-field cost line which wraps to two — `document.scrollWidth` = 440, no horizontal overflow at either width.

**The join, checked live.** Terminating PROC-02 dropped it from **both halves in the same frame** — slots 2/3 → 1/3, profile keys `[dep-1:0, dep-2:0, manual]` → `[dep-1:0, manual]`, SCRIPT COMPUTE 259.9 → 1.9 as its spend left the denominator. A row's expansion is keyed on `profileKey` and survived **50 consecutive snapshot revisions** driven at 10 Hz (`aria-expanded` still `true`, one `.process-cost-body` mounted), and terminating its neighbour did not leak the open state onto another row. Panel fold still uses the `scheduler` id, so an existing player's preference carries: 929.8 → 41.6 → 929.8 px, persisted as `["scheduler"]` in `breakout.ui.collapsed.v1`.

**The instrumentation tier still buys something visible.** With `unlocks.instrumentation` false and the scheduler unlocked — the real 480→620 lifetime-request window — the panel renders **zero** `COST DETAIL` toggles, no SCRIPT COMPUTE readout and no MANUAL RUN row, at 205.1 px against the same state's 486.2 px instrumented; the live rows are unchanged. Both grant lines were rewritten to name the panel that now exists (`… // PROCESS TABLE ONLINE`, `… // COST DETAIL ADDED TO THE PROCESS TABLE`), since the old one announced a PROCESS PROFILER that no longer has a title bar. No console errors. **329 tests (3 new in `telemetry.test.ts`**, pinning that every deployed process has exactly one profile row and that the two halves agree on all five fields, that exactly one profile row is unclaimed by a slot — the manual aggregate, found by derivation rather than by matching the name — and that TERMINATE clears both halves together**).** Screenshots were again unavailable — the Browser pane was not compositing — so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop with the store synced through the dev module graph, and every number above is from the DOM, the geometry or the snapshot.

**No GDD amendment.** The debugging tools GDD §6 lists are unchanged — the execution log, the aggregates and the plain-language report all still exist and unlock where they did. What moved is which panel they are read in, which is TDD §9's business (see its Decision Log, 2026-08-10).

**WP4b verified 2026-08-10 (in-browser, dev build).** Three controls on the row WP4a decided, one save migration (**v8**), and one deliberate split that the OP rows did not anticipate: **`PROC-nn` stays the identifier and the player's name is a designation carried beside it.** The execution log records the process name at write time, so a rename that replaced the name would leave a log split between two names for one process — the exact "join key across all three debugging surfaces" that OP-12 says must not degrade. Measured: with the ordinal renamed to `QUEUE DRAIN` mid-run, all ten `process` log entries across the rename still read **`PROC-01`**. A designation typed as `queue drain!!` was normalized to `QUEUE DRAIN` — GDD §33.3 is a promise about every player-facing string, so input is put in voice rather than refused, and only input with _nothing_ usable in it (`***`) is rejected, so it cannot silently clear a designation the player was setting. The head reads `PROC-01 QUEUE DRAIN` on one 20 px line (designation 96.9 px); the worst case the field allows — 20 W's — wraps the head to 40 px inside the 285 px panel rather than clipping.

**OP-14, measured.** HOLD on PROC-01 at 10 activations: **10 after a further 30 s** while its neighbour ran 5 → 20, with the slot (**3/4**) and the RAM (**765 MB**) unchanged and the profile row still holding its 10 activations — a held process keeps everything a player holds it in order to read. The row says `HELD — SCHEDULE SUSPENDED` and the head `HELD // 10 MB`, because a held process's last status would otherwise read as if it were still working. RESUME fired it on the next second (10 → 11, then 16 over six seconds). The two decisions the row asked for are both measured, not merely chosen: `nextDueTick` resets to now (the `advanceOffline` precedent), and `lastCondition` is cleared — a `when stats.jobs_waiting > 20` guard against a queue standing at **139** did not re-fire in 10 s (1 activation, correctly latched), and after a hold and resume fired once more (**2**). Held processes are skipped offline too, pinned by a test rather than by hand.

**OP-13, measured.** REVISE pulls the deployment's source into the editor: the CodeMirror document took `every 1 seconds { process_job() }`, the DEPLOY button became **`REDEPLOY PROC-01`**, a DROP LINK control appeared, and a line under them read `REVISING PROC-01 QUEUE DRAIN // COMMIT REPLACES IT IN PLACE // COUNTERS RESET`. The row's first flow question — pushing over an unsaved buffer — is answered without a modal: with `print(stats.cash)  # unsaved work` in the editor the button asked **`OVERWRITE BUFFER?`** in the error colour first and left the buffer untouched; the confirm state also beats `button:hover`, since the cursor is on the button at exactly the moment the warning matters. Its second question — DEPLOY adding rather than replacing — is answered by REDEPLOY being one action: committing `every 2 seconds { process_job() process_job() }` over PROC-01 kept the id (`dep-1`), the ordinal and the designation, moved the declaration to `every 2 seconds`, moved RAM 8 → 10 MB (pool 765 → 767), reset both halves of the row to **0 activations**, cleared the link, and left slots at **3/4** — not 4/4, which is what TERMINATE-then-DEPLOY would have risked. Counters reset because the aggregates describe the code that ran, and the terminal says so (`… // RAM 10 MB // COUNTERS RESET`). A revision is not refused by the space its own predecessor holds: at **4/4 slots** a plain DEPLOY of the same source was refused with `SCHEDULER SLOTS EXHAUSTED` while the REDEPLOY was admitted. A revision that does not compile changes nothing — a one-line paste was rejected with `SYNTAX REJECTED // LINE 1: …` and the resident process stayed at 31 activations with its link intact.

**Save shape.** v8 is M7.5's single migration, as planned: `label` (null) and `paused` (false) on every deployment and `run.ccl.revisingId` (null), which is exactly the situation an existing save was already in. A live save/load round trip carried the designations, the hold and the revision link unchanged (34.0 kB, `version: 8`).

**Layout.** 1280 px: panel 285 × 766.2 with four rows at 156.5 px, the three controls on one 20.4 px line (98.5 / 64.7 / 78.2 px inside a 251 px content box), TERMINATE still alone and full width below them, `document.scrollWidth` = 1280. 440 px: panel 411.2 px, controls 140.6 / 106.7 / 120.3 px still on one line, rename field 340.5 px beside a 32 px SET; the editor's three controls wrap to two rows (RUN + `REDEPLOY PROC-01` at one y, DROP LINK below, status under that — 97.2 px total) and the revision line is 383.6 × 35.2 with `scrollWidth` equal to `clientWidth` (382). `document.scrollWidth` = 440 — no horizontal overflow at either width. No console errors. **343 tests (14 new: 13 in `lifecycle.test.ts`, one v7 → v8 migration case in `save.test.ts`).**

Verified from a forced built-out state (`backup()` first, `restore()` after; the in-app browser again held only a tick-0 save, so the live save was never in the loop — it came back at tick 0, 0 lifetime requests, 0 deployments, every unlock false, exactly as taken). Screenshots were again unavailable — the Browser pane was not compositing — so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop with the store synced through the dev module graph, and every number above is from the DOM, the geometry or the snapshot. One thing worth recording for WP5: reaching the process table needed the viewport made artificially tall, because at 1280 × 720 the panel sits at y 1197–1799 in a document whose `scrollHeight` is 720. That is **OP-31**, already logged and already in WP5.

**No GDD amendment for the naming.** GDD §6's own accessibility example describes a process called "BUYER", so player-named processes were always the intent; what moved is that a process can now be **held** and **revised**, which is a tier-4 mechanic and is recorded in the Amendment Log.

**WP5 verified 2026-08-10 (in-browser, dev build).** Three rows, three separate causes, one file each side of the layering: a category on the install-channel content, a scroller on the column that never had one, and a shell that stops growing. The two design questions OP-22 left open for this package are answered here: the live channels **are** further grouped by concern (the row's own complaint is that unrelated channels run together, so splitting only live from exhausted would have left the twelve-entry reading problem intact), and the section names are `THROUGHPUT`, `CAPACITY`, `PROCESS EXECUTION` and `POWER AND COOLING`, which name what the package does to the node rather than the effect kind — grouping on `effect.kind` gives twelve groups of one. Category and section order are `/content`, so the panel infers nothing.

**OP-31, measured at 1280 × 720**, on a built-out state with terminal, trigger, editor and execution log all expanded, and measured **both ways in the same session** — with the fix, and with its two new declarations overridden back off, which reproduces the pre-fix geometry exactly. **Before: `.terminal-output` is 27.5 px tall while holding 50 lines**, the terminal panel squeezed to 84 px, and the column's scroll range is **0** — the squeeze lands entirely on the one `flex: 1` child, so nothing is "clipped" only because the terminal has been crushed instead. **After: the terminal panel holds its 10rem floor (160 px), the output is 103 px**, the execution log's bottom sits 75.5 px past the column bottom — and the column now scrolls **76 px** to reach it, ending with the log fully inside the viewport (bottom 651.2 px against a column bottom of 651.6 px). Page scroll range is still 0 at desktop widths, which is why the column needed its own scroller rather than the document's.

**OP-22, measured.** Twelve channels revealed, nine live and three exhausted (BATCH AGGREGATION WINDOW 4/4, MEMORY PARTITION GRANT 3/3, ITERATION BUDGET EXTENSION 1/1). The live nine render in four sections — THROUGHPUT (3), CAPACITY (2), PROCESS EXECUTION (2), POWER AND COOLING (2) — each under a 21.8 px label; the exhausted three collapse into `CHANNELS EXHAUSTED // 3`, folded by default at **14 px**, one 21.6 px line each when opened, and the panel renders **zero** `CHANNEL EXHAUSTED` buttons where it used to carry one per exhausted channel forever. The fold is worth 278.4 px of a side column: the same twelve channels with none exhausted measure **1368.2 px**, against **1089.8 px** with three folded away. Default-collapsed state persists through the same key as every other panel (`breakout.ui.collapsed.v1`), and the set now stores the ids whose state **differs from that panel's default** — opening the group writes `["upgrades-exhausted"]`, closing it back writes `[]`. Existing panels are unaffected: PROCESS TABLE still writes `["scheduler"]` when folded and `[]` when opened.

**OP-24, measured at 1920 px.** Before: shell 1920 wide, terminal column **1556 px**, side column **300 px**, install panel 285, market chart 251. After: the shell caps at **1560 px and centres** (left edge 180), terminal column **1102.9 px**, side column **393.1 px**, install panel 378.1, market chart 344.1. The width goes where the content wraps: across the nine live channels the descriptions took **15 rendered lines before and 11 after**, and the worst entry dropped from 3 lines to 2. At 1280 the side column moves 300 → 320.3 px and the terminal column 916 → 895.7; at 800 px the side column sits on its 300 px floor exactly as before, so narrow desktops are unchanged.

**Layout.** 1280 × 800: shell 1280, terminal column 895.7, side column 320.3, install panel 305.3 × 1089.8, `document.scrollWidth` = 1280. 440 px: single column at 411.2 px, install panel 411.2 × 1034.6, all four section heads on one 21.8 px line, exhausted rows one line each (longest name 175.2 px), the document scrolling 2776 px as the ≤760 px path already intended, and `document.scrollWidth` = 440 — no horizontal overflow at 440, 800, 1280 or 1920. No console errors. **348 tests (5 new in `upgrades.test.ts`,** pinning that every channel carries a declared category, that no declared section is empty, that the categories **partition** the revealed channels — a channel in no section would silently vanish from the panel rather than fail — that the view publishes the content category, and that live/exhausted is exactly the `nextCost === null` split**).**

Verified from a forced built-out state (`backup()` first, `restore()` after; the in-app browser again held only a tick-0 save, so the live save was never in the loop — it came back at tick 0, 0 lifetime requests, 0 deployments, exactly as taken). Screenshots were again unavailable — the Browser pane was not compositing — so the sim was driven by an explicit 10 Hz `engine.tick(100)` loop with the store synced through the dev module graph, and every number above is from the DOM or the geometry.

**No GDD amendment.** Nothing about what the player buys, or what it costs, or the voice, moved: this is which panel holds what and how wide it is, which is TDD §9's business (see its Decision Log, 2026-08-10). **OP-28 is untouched and still open** — grouping tidies a list that is separately revealing entries 2.2–4× ahead of affordability, and that is M8's balance pass.

**OP-26 was weighed and left open, because its stated cheap fix is wrong.** The row proposes "a tier-0 template that generates a bare command sequence (content-only, one entry in `/content/templates.ts`)". It is not content-only: a bare sequence is only worth offering if its length is a parameter, and `renderTemplate` does `{{param}}` substitution — it cannot repeat a line _n_ times, so a count parameter needs repetition in the renderer, and without one the template is a fixed-length snippet that is worse than nothing. The row's other candidate — moving the conditions grant from 320 nearer the editor's 200 — is a pacing change to the tier ladder and belongs to M8's balance pass, not to a package whose scope is two named rows. Both are real options; neither is the cheap content addition the row records, so the row is annotated and carried rather than silently re-scoped.

**Ordering constraints — not preferences:**

- **WP1a and WP1b before WP6.** A tester who leaves at the opening screen answers nothing; the editor does not appear until 200 lifetime jobs.
- **WP1a before WP1b, and the split is by design dependency, not by phase.** WP1a is everything about the opening screen that is true whatever the briefing turns out to say — the first-click `NO REQUESTS QUEUED` fault (the queue starts empty and fills at 0.9/s, so the player's first feedback is an error), and the missing motion of OP-16. WP1b is the briefing itself: §34's open questions answered, the design committed to the GDD, the copy written and implemented. Deliberately **not** split design-session/build-session: onboarding is hard to judge on paper, and committing a design to the GDD that implementation then disproves leaves a permanent wrong entry in a log that forbids rewriting history. The boundary also exists so the briefing's tone can be reviewed before it is built, since that is an owner decision (§33.3), and so WP1b can be redone cheaply — first-contact copy is normally written three times.
- **WP1a establishes the fresh-save verification routine.** Onboarding is the first thing in this project that can only be checked from a cold start: `backup()`, clear the save, play the first sixty seconds as a stranger would, `restore()`. Working that out on the small package is deliberate.
- **WP2 before WP6.** Templates are the _whole_ interface for a non-programmer, and today that interface has a buy side and no sell side.
- **WP4a before WP4b.** OP-12/13/14 all add controls to whichever process panel survives OP-25. Deciding the panel afterwards means building those controls twice.
- **WP4b under one save migration.** OP-14 needs a flag on `DeploymentState`; do the three together so M7.5 costs one version bump (v8), not three. M8's meta/run split then lands as v9 (TDD §8).
- **WP5 settles OP-24's column width and OP-22's grouping together** — grouping changes how much width the install list needs, so they are one decision, not two.
- **WP6 before M8's balance pass, not after.** M8 task 5 tunes the whole game towards a pacing target; tuning towards a loop no outside player has validated risks polishing the wrong thing.

**Scope added on 2026-08-10, recorded rather than folded in silently.** The milestone's own risk row says a new item goes to "Open items" and waits for M8 unless it blocks a package outright, and that adding one to M7.5 means displacing one. Two of the five items raised in owner play on 2026-08-10 were admitted, both as **defect fixes rather than new features**, and neither displaces anything: **OP-27** (the terminal forces itself to the newest line ten times a second, so it cannot be scrolled back) joins **WP3**, because a readout that cannot be read is the same failure as a readout that lies, and because a tester who cannot re-read what a script printed cannot give M7.5's own acceptance criterion a fair hearing; **OP-31** (the left column has no scroller, so expanded panels are clipped where nothing can scroll to them, and the terminal renders at zero height) joins **WP5**, which already owns the shell geometry and edits the same file. The other three were routed outward: **OP-28** to M8's balance pass, **OP-29** to the post-prototype group with OP-9/OP-17/OP-21, **OP-30** to a candidate M9 pending a GDD decision.

**The cut line, if this has to be shortened:** WP1a, WP1b, WP2 and WP3 are non-negotiable — without them a tester is being asked to judge a game that does not say what to do, has a one-directional template interface, and visibly shakes. WP4 buys the _quality_ of the evidence that comes back. WP5 is comfort and may be cut to a follow-up — **except OP-31, which must come out of it and be done anyway if WP5 is dropped**: a High-severity defect that hides the terminal entirely cannot ship because it happens to live in the package that got cut.

**Deliberately not in scope** (they stay open in the table below and are re-read when M8 opens): OP-5 (bundle size), OP-8 and OP-10 (fold into M8 tasks 1 and 5 respectively — cheaper inside a save rewrite and a balance pass than as standalone work), OP-9 and OP-17 and the **attribution half of OP-21** (one coherent post-prototype piece: the profiler cannot diagnose failures that are not execution failures, and per-deployment resource flow is the plumbing all three need), OP-18 (latent), OP-23 (its value drops once OP-24 lands — revisit after, and only if still wanted).

**Acceptance:** a first-time player reaches the script editor unaided; a player holding a deployed process can tell it apart from its neighbours, pull its source back for editing, stop it without destroying its counters, and read from the resource panel whether their automation is holding — none of it requiring developer intervention or a look at the source. Verified in-browser at 1280 px and 440 px, with numbers recorded, as every milestone before it.

### Session prompt

M7.5 is built one package per session. The prompt below is the entry point; it is reproduced here so it survives the session that wrote it.

```text
Work the next package of milestone M7.5 in BREAK//OUT.

1. Read CLAUDE.md, then Docs/ImplementationPlan.md — the M7.5 section is the scope.
   Read the OP rows your package names, and the GDD/TDD sections those rows cite.
2. Pick the FIRST package in table order whose status is not 🟢. If one is already 🟡,
   continue that one instead of starting another. Do exactly one package per session.
   WP1b must not start before WP1a is 🟢; WP4b must not start before WP4a is 🟢.
3. Mark it 🟡 in the plan before you start.
4. Build it. Balance numbers live in /src/content; layering is ui → core → ccl and
   core → content; /core, /ccl and /content stay pure. Add or extend at least one test.
5. Verify in the browser at 1280 px and 440 px on the dev server, using window.__breakout
   (backup() first, restore() after — never touch localStorage by hand). Record what you
   actually observed: numbers, not adjectives. WP1a and WP1b additionally require a cold
   start — backup(), clear the save, play the first sixty seconds as a stranger would,
   then restore() — because onboarding cannot be judged from a built-out save.
6. Update the docs in the SAME session — mark the package 🟢 with its verification note,
   close the OP rows it resolves (set 🟢 and append a dated note inside the row; never
   delete a row), append a Change log entry, add a TDD Decision Log entry for any
   architecture/engine/save-shape change, and a GDD Amendment Log entry if scope,
   mechanics or tone moved.
7. Gates, all four, before committing:
   npm test && npm run lint && npm run format:check && npm run build
8. Commit naming the package and the OP items closed. Do not push unless I ask.

If the package turns out to be materially larger than the plan says, or you find that an
OP row's stated cheap fix is wrong, stop and tell me before building — do not silently
re-scope. Finish by reporting what changed, what you verified, and what is still open.
```

---

## M8 — Prestige fork + full-run balance pass (GDD §22, §31)

**Goal:** One complete prototype run, end to end, with a reason to run again.

| Task                    | Detail                                                                                                | Status |
| ----------------------- | ----------------------------------------------------------------------------------------------------- | ------ |
| Meta/run save split     | `meta` survives fork, `run` wiped (TDD §7, §8)                                                        | 🔵     |
| Fork milestone + screen | Diegetic "train successor architecture" moment and confirmation flow                                  | 🔵     |
| Architecture points     | Earned from lifetime stats; 2–3 permanent modifiers purchasable                                       | 🔵     |
| New-seed second run     | Fork reseeds world; kept constructs make run 2 faster and different                                   | 🔵     |
| Full balance pass       | Target: first fork reachable in ~2–4 hours of play; pacing per GDD §23 early rows                     | 🔵     |
| Playtest round          | ≥3 external playtesters incl. at least one non-programmer using templates only; findings logged below | 🔵     |
| Prototype verdict       | Written answer to the exit question, appended to this document                                        | 🔵     |

**Acceptance:** A tester can play from first click through regime shift, thermal challenge and one fork without developer intervention, and reports wanting to continue.

---

## Cross-cutting requirements (apply to every milestone)

- **Docs stay current:** update this file's status tables and the TDD Decision Log as part of finishing any task — not afterwards, not in batch.
- **Determinism guard:** every milestone adds/extends at least one seeded scenario test.
- **No balance literals in `/core`** (TDD §11).
- **Diegetic voice everywhere:** all player-facing errors and events use the hard-sci-fi system voice (GDD §33.3); no jokey placeholder text, because placeholder text has a way of shipping.

## Risks and watch-items

| Risk                                                               | Mitigation                                                                                                                                                                                          |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The core bet fails (programming isn't fun for non-programmers)     | Template mode lands in M4, not at the end; playtest with a non-programmer no later than M6.                                                                                                         |
| CCL scope creep (temptation to add functions/`while`/arrays early) | Language surface is frozen to TDD §5.1 until the M8 verdict; log desires in the backlog below.                                                                                                      |
| Defects found mid-prototype get forgotten                          | Log them in "Open items from prototyping" below; review that list when opening each milestone.                                                                                                      |
| Balance churn eats the schedule                                    | All numbers in `/content`; scenario tests pin pacing; timebox balance passes.                                                                                                                       |
| Terminal UI charm vs. usability                                    | Playtest readability early (M2); aesthetic never blocks legibility.                                                                                                                                 |
| M7.5 turns into an open-ended polish milestone                     | Its scope is exactly the OP rows named in its table. A new item found during it goes to "Open items" and waits for M8 unless it blocks a package outright; adding one to M7.5 means displacing one. |

## Backlog (post-prototype, do not start)

Deferred per GDD §31: functions, `while`, collections/history tier, threads, agents, block mode, additional market regimes/competitors, datacentres, escape layers, further prestige depth, sound design, mobile layout.

Added 2026-08-10: **replay modifiers for a second run** — new-game+ conditions that make a run harder or differently shaped (a standing RAM shortage, raised install costs) rather than faster. Proposed as a post-M8 milestone; tracked as **OP-30** and needs a GDD amendment before it becomes one, since M8's prestige fork only carries modifiers that make run 2 _easier_.

## Open items from prototyping

Known defects, limitations and deliberate shortcuts found while building the prototype, which are **not** scoped to a milestone. Unlike the Backlog above, these are not "do not start": any of them may be pulled into the current milestone at any time if it starts obstructing play or testing. Whatever is still open when the M8 verdict is written becomes the starting point for the post-prototype backlog.

Rules: append new rows as items are found — including ones found and immediately fixed, recorded as `Closed` with the commit that did it. Do not delete rows; close them. `Severity` is about impact on the prototype's exit question ("is clicker + programming fun?"), not engineering neatness.

| #     | Date       | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Severity | Status |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------ |
| OP-1  | 2026-08-08 | **Editor status line keeps a stale fault after a successful DEPLOY.** `compileSource` writes `run.ccl.lastRun` on a parse failure and is shared by RUN and DEPLOY; a successful deploy returns without touching it, and only a subsequent RUN clears it. So a rejected deploy leaves `LAST RUN // FAULT — …` on screen indefinitely while later deploys succeed. Two faults in one: DEPLOY should not be writing a field labelled "LAST RUN" at all, and a successful action must clear the previous failure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | High     | 🟢     |
| OP-2  | 2026-08-08 | **Panels cannot be collapsed.** Only 3 of 11 UI components have a toggle (research feed, API reference, template library). Terminal, EXECUTE, resources, process monitor, profiler, execution log and install channels are always fully expanded, so the page grows monotonically as tiers unlock and panels that are irrelevant right now cannot be got out of the way. Worst on narrow screens, where everything is one column. **2026-08-08:** every panel is now a collapsible `Panel` with a persisted per-panel state; at 440 px the page went from 1939 px tall to 900 px with everything folded.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Medium   | 🟢     |
| OP-3  | 2026-08-08 | **Job supply is the binding constraint at the loop tier, and the queue buffer cannot be raised.** `jobs.queueCapacity` is a flat 60 with no upgrade effect touching it, while a tier-6 loop can ask for up to 100 `process_job()` calls in a single activation. Arrival _rate_ is upgradeable (REQUEST ROUTING UPLINK, ×1.5, max 3) but the _buffer_ is not, so burst consumption can never find more than 60 jobs waiting however much the player invests. Observed during M5 verification as `STARVED // 217 of 230 requests rejected`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | High     | 🟢     |
| OP-4  | 2026-08-08 | **BATCH DRAIN template caps `repeats` at 10**, matching the base iteration limit, so the template cannot use ITERATION BUDGET EXTENSION at all — a player who buys the upgrade must hand-edit the generated code to benefit. Deliberate simplification when the template was written; template params have no access to derived stats. **2026-08-09 (M7.5 WP2): closed.** A parameter may now declare `maxFrom`, naming a live derived stat that supplies its ceiling instead of a literal; `paramMax`/`clampParam`/`renderTemplate` take a `TemplateLimits` argument the caller reads from the snapshot, so `/core/templates.ts` stays pure. `repeats` declares `max: BALANCE.ccl.iterationLimitBase` with `maxFrom: 'iterationLimit'`, so the starting value cannot drift from the limit it names, and the form states the ceiling in its label. Verified live: the control read `MAX 10`, moved to `MAX 100` the moment ITERATION BUDGET EXTENSION was installed and with no reload, generated `for i in range(100)`, and clamped a request for 500 back to 100.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Low      | 🟢     |
| OP-5  | 2026-08-08 | **Bundle is 561 kB minified, over Vite's 500 kB warning** (CodeMirror dominates). Accepted in the TDD Decision Log 2026-08-06 with the trigger "revisit if load time ever matters for playtests" — recorded here so that trigger has somewhere to be noticed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Low      | 🔵     |
| OP-6  | 2026-08-08 | **Non-programmer playtest is committed to but not scheduled.** The Risks table above says "playtest with a non-programmer no later than M6", but it is mitigation prose, not a task with a status, so M6 can be closed without it. The Playtest findings log is still empty. **2026-08-08:** now a tracked 🔴 task in the M6 table and a carried-forward blocker on the "Current status" line, so it can no longer be closed silently — but it still needs an external tester, so the item stays open.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | High     | 🟡     |
| OP-7  | 2026-08-08 | **Forcing state through `window.__breakout.engine` destroys the playtest save.** A backup taken into another `localStorage` key was overwritten during this session's M5 verification, because the on-unload autosave commits the crafted in-memory state over the restored file before the reload completes. CLAUDE.md warns to back up first but the warning is not sufficient — the restore has to survive unload, e.g. export the archive to a file, or restore _after_ load rather than before. **2026-08-09:** the dev handle now exposes `backup()` / `restore()` / `persist()` / `dump()`, and neither helper touches `localStorage` directly — `restore()` goes through `engine.load` and _then_ persists, so the autosave has nothing stale left to commit. Exercised in this session's M7 verification: the live playtest save was backed up, a fully built-out state forced over it, and the original restored intact.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Medium   | 🟢     |
| OP-8  | 2026-08-08 | **The save file is now ~44 kB** (was ~6.5 kB at M5). Breakdown measured in the browser: execution log 25 kB, market price history 11.3 kB, terminal tail 4.5 kB. Well inside `localStorage` limits, but it makes the base64 export/import archive unwieldy for playtesters and will matter at the IndexedDB move (TDD §2). Cheapest fixes: round history samples on serialization, and stop persisting the full execution log (or trim it to the last N).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Low      | 🔵     |
| OP-9  | 2026-08-08 | **A strategic failure produces no profiler diagnosis.** Verified in-browser: after the regime shift the naïve trader loses capital with 0 rejections, 0 aborts and `lastStatus: ok`, so `diagnose()` correctly reports nothing — every execution counter is healthy. The only surfaces that say something is wrong are the market panel's NET readout and the `market-loss` narrative beat. That is defensible (GDD §6 diagnoses execution failures) but it leaves the prototype's best debugging tool silent for the class of failure M6 exists to teach. Candidate: a per-process capital-flow column, so a process that spends more than it earns is visible in the profiler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Medium   | 🔵     |
| OP-10 | 2026-08-08 | **`market.average` costs the same whatever window it reads.** The listed cost is a flat 0.1 compute, so `market.average("compute", 300)` is priced identically to `market.average("compute", 2)` even though it walks 150× more history. Harmless now (the ring buffer is small and the op-fuel charge dominates), but it means the trend-filtered adaptation is effectively free, and it will misprice the tier-7 collections/history work that reuses the same idea.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Low      | 🔵     |
| OP-11 | 2026-08-08 | **The API reference names parameters but never says what may go in them.** Raised in review of `market.price(good)`: the panel shows a signature and one prose sentence, so `good` is an unexplained word. Coverage is inconsistent rather than absent — `market.price` happens to say "compute" or "energy" in its description, while `market.average(good, n)` says only "a good" and never mentions that `n` is capped at `market.maxAverageSamples` (300). The trade commands never mention the `maxOrderUnits` (1000) ceiling. `print(value)` is the general case: "Write a value to the terminal" does not tell the player that a value is a number, text in quotes, a yes/no, or nothing — nor that, because CCL has no truthiness, `print(stats.cash > 10)` legitimately prints `true`. **The real domains exist only inside `validate()` in `/core/registry.ts`**, as misuse messages ("…is not traded here. Known goods: compute, energy."), so the only way to learn what an argument accepts is to get it wrong first. That is discovery-by-error, which is the opposite of the GDD §6 accessibility rule the misuse messages themselves were written to serve. Structural, not a wording fix: `CclCommandDoc` has no per-parameter field, so both the reference panel and autocomplete (`detail: signature`, `info: desc`) can only ever show what it holds — a fix means giving each parameter a name plus a stated domain in `/content/cclApi.ts`, sourced from the same balance numbers the validators use so the two cannot drift. Most likely to be caught by the pending non-programmer playtest (OP-6) — worth fixing before it, not after. **2026-08-09:** `CclCommandDoc` gained a `params` list, each parameter carrying a name and a stated domain, rendered in the reference panel and in autocomplete; bounded domains are composed from the same `BALANCE` values `validate()` checks, and a test pins the documented parameters against each signature in both directions. `print(value)` now says a value may be a number, quoted text or a yes/no, with `print(stats.cash > 10)` as the worked example.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Medium   | 🟢     |
| OP-12 | 2026-08-08 | **Deployed processes cannot be named, and `PROC-nn` does not identify them.** Reported in play: with several processes deployed it is not possible to tell PROC-05 from PROC-06. The name is generated from `run.scheduler.nextId`, which is monotonic and **never reset by TERMINATE**, so a few deploy/terminate cycles leave gappy, meaningless ordinals with no relation to what the process does (PROC-07 appeared during M6 verification after two redeploys). This is not cosmetic: `deployment.name` is the join key across all three of the prototype's debugging surfaces — the process monitor, the execution log (`PROC-01 every 1 seconds // …`) and the profiler rows — so unlabelled processes degrade the M5 "debugging as gameplay" deliverable exactly when it matters most, i.e. once there is more than one process to reason about. The declaration header shown beside the name (`every 2 seconds`) is a partial mitigation but does not separate two processes on the same interval, which is the common case. Up to four can be resident (1 base slot + 3 PROCESS TABLE EXTENSION installs). Cheap: `name` is already a persisted field on `DeploymentState`, so this is an action plus an input, with no save migration — the design questions are whether the name is set at deploy time or renamable afterwards, and what happens to the diegetic voice if a player types something that is not in it. **2026-08-10 (M7.5 WP4b): closed, and not by renaming the field this row names.** `PROC-nn` stays the identifier and a player-set **designation** is carried beside it (`DeploymentState.label`), because this row's own argument decides it: `name` is the join key the execution log writes at activation time, so replacing it would split a process's log between two names — degrading exactly the M5 deliverable the row is defending. Measured with a rename mid-run: all ten `process` log entries either side of it still read `PROC-01`. Renamable afterwards rather than set at deploy, so the deployments a player already holds can be told apart without redeploying them. The voice question is answered by normalizing rather than refusing: input is upper-cased, reduced to the terminal's own character set, collapsed and cut to `BALANCE.scheduler.processLabelMaxChars` (20) — `queue drain!!` became `QUEUE DRAIN` — and only input with nothing usable left (`***`) is rejected, so a designation cannot be silently cleared by a typo. The head reads `PROC-01 QUEUE DRAIN` on one line at both 1280 px and 440 px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Medium   | 🟢     |
| OP-13 | 2026-08-08 | **A deployed process can only be terminated, not edited.** There is no way to get a running process's source back into the editor, so iterating on deployed automation means retyping it, re-deriving it from a template, or terminating blind. The process monitor renders only a TERMINATE button. The pieces are already in place: `DeploymentView.source` is **already carried in the snapshot** and rendered nowhere, and `EditorPanel.handleInsertTemplate` already does exactly the required push (`view.dispatch` + `SET_EDITOR_SOURCE`) — so this is UI wiring, not a core change. Two flow questions to settle before building it, because the obvious version is confusing: (1) pushing a deployment's source over the editor discards whatever unsaved buffer is there, so it needs a guard or an undo; (2) DEPLOY **adds** a deployment rather than replacing one, so edit-then-deploy silently leaves the original running and consumes a second slot — with only four slots that bites immediately. The likely right shape is a REDEPLOY that terminates and deploys as one action, keeping the process's name (see OP-12) and making clear whether its counters reset. **2026-08-10 (M7.5 WP4b): closed, in the shape the row predicted.** A per-row `REVISE` pulls the source into the editor and records the target on `run.ccl.revisingId`; the editor's DEPLOY button then reads `REDEPLOY PROC-nn`, with a `DROP LINK` control and a line stating what committing does. It is not quite the UI-only change the row expected — CodeMirror owns its document and cannot re-render from the snapshot, so the sim emits an `EDITOR_SOURCE_SET` event the editor re-syncs on, the same way it already did for a load. Flow question (1) is answered without a modal: the button asks `OVERWRITE BUFFER?` first, and only when the buffer is non-empty and differs. Flow question (2) is answered by REDEPLOY being one action that replaces in place — the deployment id, ordinal and designation survive, the counters reset (the aggregates describe the code that ran, and the terminal says `COUNTERS RESET`), and the old copy cannot be left running: measured at 4/4 slots, a plain DEPLOY of the same source was refused while the REDEPLOY was admitted, because the process being replaced is discounted from the slot and RAM check. A revision that does not compile changes nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Medium   | 🟢     |
| OP-14 | 2026-08-08 | **A running process cannot be paused — only terminated.** TERMINATE is destructive: `undeployScript` splices the deployment out, so its profiler row and every counter behind it (activations, ops, compute share, failure ratios, the plain-language diagnosis) disappear with it. The execution-log entries survive, since they live in `run.telemetry` keyed by name, but the aggregate view the player was reasoning with does not. Two consequences. **As a debugging tool:** disabling one process at a time is the standard way to isolate which one is causing something, and the prototype's answer to "debugging as gameplay" (GDD §6) currently cannot do it without throwing away the evidence. **As a response to M6:** after the regime shift a naïve trader actively drains capital — measured at −346 CR over 900 s — and the player's only lever is to destroy it. The milestone deliberately creates that problem and does not supply the calm response to it (stop the bleeding, read the profiler, edit, resume). Cheapest fix is a flag on `DeploymentState` gating `stepScheduler` and `runOfflineProcesses`; a paused process should keep its slot and RAM footprint, or "pause" is TERMINATE with extra steps and the slot scarcity that makes GDD tier 4's "which systems deserve permanent automation" a real choice evaporates. Three decisions to settle: what `nextDueTick` means on resume (`advanceOffline` already resets it to "now", which is probably the precedent); whether a `when` guard's `lastCondition` re-arms on resume, since a standing-true condition would otherwise never re-fire; and whether the save-shape change takes a v7 migration or defaults a missing flag to "running" (TDD §8 says migrations are added, never skipped). Cheapest of OP-12/13/14, and the one that most directly serves the exit question. **2026-08-10 (M7.5 WP4b): closed.** A `paused` flag on `DeploymentState` gates `stepScheduler` and `runOfflineProcesses`, at the deployment rather than per declaration — a held process runs nothing and samples nothing, so a `when` guard stops drawing polling fuel while held. It keeps its slot and its RAM, as the row requires: measured at 10 activations, still 10 after a further 30 s while its neighbour ran 5 → 20, with slots at 3/4 and RAM at 765 MB unchanged and the profile row intact. All three decisions the row lists are settled and pinned by tests: `nextDueTick` resets to now on resume (the `advanceOffline` precedent), `lastCondition` is cleared so a standing-true guard fires the crossing it is owed (measured: a guard against a queue at 139 did not re-fire in 10 s, then fired once on resume), and the save-shape change takes a real migration — **v8**, defaulting a missing flag to running, shared with OP-12's `label` and OP-13's `revisingId` so M7.5 costs one version, as planned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Medium   | 🟢     |
| OP-15 | 2026-08-08 | **A new player does not know what they are supposed to do.** First reported playtest finding (see the Playtest findings log): the opening screen does not communicate a goal or a first action, and the tester asked for a tutorial. Grounded in the code, the opening state is three lines of boot flavour, an `EXECUTE` button and a compute meter — everything else is concealed by design (capital readout appears only once capital exists, system readouts at 10 lifetime jobs, the first install at 15, the script editor at 200). The single instruction is `MANUAL INFERENCE TRIGGER ARMED. AWAITING OPERATOR INPUT.`, which is in-fiction but never says what pressing the trigger achieves or what the player is working toward. So GDD pillar 2.5's diegetic progressive reveal — a strength later — is producing an empty, unexplained first screen. One concrete first-contact bug sits inside this: the queue starts empty and fills at 0.9/s, so a player who clicks within the first second gets `NO REQUESTS QUEUED // INFERENCE TRIGGER IDLE` — an **error message as their first feedback**. **This is a design gap before it is an implementation gap:** the GDD has no onboarding section at all (a search for tutorial/onboarding/first-time across all four documents returns nothing), so there is no documented intent to implement against. Whatever is built must not fight GDD §33.3's voice — a modal "click here!" would wreck the aesthetic — but the fiction already supplies the vehicle, since the system addresses an _operator_ and a system instructing its operator is in-voice. **Severity is High because this precedes everything:** the prototype's exit question cannot be answered by a player who leaves before the script editor unlocks at 200 jobs, which is the entire thing being tested. **2026-08-09 (M7.5 WP1a, partial):** the concrete first-contact bug named inside this row is closed — a run opens with three requests already queued, so the first press of the trigger always processes work, and an empty-queue press now answers with the wait until the next arrival in the system voice rather than as a fault. An 85 s cold-start session produced no `error` line at all. The row stays open because the rest of it is the design gap: the opening screen still states no goal and no first action, which is WP1b's subject (GDD §34). **2026-08-09 (M7.5 WP1b): closed.** The design gap is closed as a design first — GDD §34 now carries the onboarding design, its three open questions answered and its requirements marked met — and then as an implementation: a **standing operator directive** above the terminal posts one objective at a time (action, what it produces, what completing it releases, with a counter running towards the threshold it is quoting), and three briefing lines at boot say what the node is, what a cleared request produces, and that capability is released against throughput — automation first, then the scripting interface. That last sentence is the promise the 200-request wall needed and is the only place anything unearned is named; each directive looks exactly one release ahead, so pillar 2.5's reveals are intact. The set retires at the first RUN. Skipping is folding the panel, which puts the preference outside the save. Verified from a cold start at both widths (see the WP1b note above): sixty seconds of first-contact play produced 68 cleared requests, 17.00 CR and zero `error` lines, with the directive advancing on its own at the fifteenth. | High     | 🟢     |
| OP-16 | 2026-08-09 | **The temperature readout no longer flickers when the sandbox is idle.** M7 replaced the cosmetic ±0.6 °C band with the real heat model, so a node doing no work now reads a perfectly static 34.0 °C. On balance this is an improvement — the reading responds to the player's own clicks (+1.08 °C for a 4-request batch, decaying back over ~10 s), which is meaningful feedback rather than noise — but the opening screen has lost a moving element it used to have, and per OP-15 that screen has none to spare. Recorded as a deliberate trade rather than a defect; worth revisiting together with whatever onboarding work OP-15 produces. **2026-08-09 (M7.5 WP1a): closed, and not by restoring the flicker.** Two findings while fixing it. First, the temperature readout was never on the opening screen at all: it lives behind `unlocks.systemReadouts`, which is granted at 10 lifetime jobs, so the cosmetic flicker was motion on the _second_ screen, not the first. Second, faking motion in a number that is now a real physical model would be a regression — the reading responds to the player's own clicks (34.0 °C at rest, 39.3 °C after 71 requests), and noise on top would corrupt a value the thermal challenge later asks the player to control. Motion was therefore added where the problem actually is, out of state that is already real: an **INBOUND meter** in the trigger panel, filling towards the next arriving request off the sim's fractional arrival accumulator and resetting every ~1.1 s at the opening rate. It also carries the rate and the countdown, so it answers the question an empty queue raises rather than only decorating.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Low      | 🟢     |
| OP-17 | 2026-08-09 | **A `when` guard that fires once and then never re-arms produces no diagnosis.** Found while tuning M7 and now pinned by a test: `when stats.temperature > 68 { boost_cooling() }` fires on the crossing, and because a single coolant hold cannot pull an 85 °C core back under 68 °C, the guard never goes false, never re-fires, and the node overheats anyway. The semantics are correct (edge-triggering is the M4 design, and it is what stops a standing condition re-firing for free) and the failure is real control engineering — but the profiler cannot describe it. `diagnose()`'s `silent` report covers a guard that has _never_ been true; there is no report for one that fired once and stopped, which from the counters looks like a perfectly healthy process with one activation. The player's only clue is the temperature. Same family as OP-9: an execution-healthy process failing at its actual job. Candidate: a report keyed on a `when` process whose guard has been standing true for far longer than its activation count implies.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Medium   | 🔵     |
| OP-18 | 2026-08-09 | **Clock ticks are fractional after an offline catch-up.** `advanceOffline` advances `thermal.clockTicks` (and, since M6, `market.clockTicks`) by `chunk × TICKS_PER_SEC`, where the last chunk of a catch-up is usually a fraction of a second — so a save can carry `clockTicks: 216661.07`. Harmless today: every consumer treats the value as continuous time, and the demand-window and seasonal maths are both continuous. It is logged because "ticks" reads as a whole number, so anything that later assumes integrality (a replay, a modulo over ticks, a serialized-size optimisation) would be quietly wrong.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Low      | 🔵     |
| OP-19 | 2026-08-09 | **The POWER DRAW readout flickers in and out when the energy reserve sits at zero, and the whole side column jumps with it.** Reported in play. `ResourcePanel` renders the row only `{resources.energy.ratePerSec < 0 && ...}` (`src/ui/components/ResourcePanel.tsx`), so the row _mounts and unmounts_ rather than changing value — and every appearance changes the panel's height, shifting the thermal, market, monitor, profiler, upgrade and research panels below it. The trigger is the M2 energy model working as designed: an empty reserve throttles daemon throughput by `energyThrottledFactor` instead of halting it, which cuts the drain, which can make `energyRegenPerSec - drainPerSec` non-negative for a tick, which lets the reserve refill, which un-throttles and makes it negative again. The TDD Decision Log (2026-08-06) explicitly accepts that full/throttled oscillation as groundwork for the GDD §6 feedback-instability teaching, so **the sim is not the bug — the readout is**, and at a 10 Hz tick the layout can move ten times a second. M7 makes it easier to reach, because the coolant boost adds a sustained draw on top. Cheapest fix is to stop the row from being conditional: always reserve the space once system readouts are unlocked, dim it while the balance is positive, and show the sign. Whatever is done should not smooth the underlying number away, since the oscillation itself is something the player is eventually meant to notice. **2026-08-10 (M7.5 WP3): closed, and not by making the row unconditional.** There is no row any more: the energy balance is a signed rate on the ENERGY meter's **own label line**, present from the moment system readouts are granted, so nothing can mount or unmount and the panel's height is fixed by its meter count. Negative keeps the error colour POWER DRAW carried, so no signal the old row gave is lost. The underlying number is untouched, as the row asks: with the reserve resting at 0.13 of 400 the sign flipped on **199 of 200 consecutive ticks** (100 negative, 100 positive) and the readout alternated `-3.08/S` and `+1.33/S` tick by tick — while across those same 200 samples the resource panel measured **one** height (241.51 px), the panel below it **one** top (321.36 px), and the panel held 5 rows throughout.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Medium   | 🟢     |
| OP-20 | 2026-08-09 | **Template mode has no way to buy energy and no way to sell a saturated pool.** Reported in play, and independently hit during M7 verification. The template library covers buying compute (BUFFER TOP-UP) but there is **no energy equivalent and no sell template at all** — `sell_compute`/`sell_energy` appear in no template, so a template-only player has a buy side and no sell side. Two concrete consequences. **The thermal governor is unusable in template mode alone:** `boost_cooling()` draws more power than the sandbox generates at the build-out where demand windows bite, so M7's answer to its own challenge only works when paired with an energy supply — during verification the governor had to be paired with a hand-written `every 2 seconds { if stats.energy < 400 { buy_energy(100) } }` before it held. **A saturated pool silently throws capital away:** a buy that overflows is charged in full and the surplus discarded (TDD §6, deliberate), and daemon income into a full compute buffer prints `COMPUTE BUFFER SATURATED // SURPLUS CREDITS DISCARDED` indefinitely — the player can see the waste and, without typing code, cannot act on it. Nothing new is needed to fix this: `stats.compute_capacity` / `stats.energy_capacity` exist (M6) and tier-3 arithmetic already expresses `stats.compute_available > stats.compute_capacity * 0.9`, so a RESERVE TOP-UP (good as a parameter, or one template per good) and a SURPLUS SELL are content-only additions to `/content/templates.ts`. This sits directly on the GDD §25 accessibility path and therefore on the pending non-programmer playtest (OP-6): the templates are the _whole_ interface for that tester. **2026-08-09 (M7.5 WP2): closed.** Three content-only additions: **RESERVE TOP-UP** buys energy against a floor, and **COMPUTE SURPLUS SELL** / **ENERGY SURPLUS SELL** sell a pool back to the exchange while it sits above a fraction of its own capacity — a fraction rather than an absolute level, so the setting survives an ENERGY BUFFER CELL install and so the generated code shows the player that capacity is readable. One template per good rather than a good-as-parameter selector: a selector would have to substitute identifiers, not just a quoted string (`sell_compute` vs `sell_energy`, and `stats.compute_available` vs the non-parallel `stats.energy`), which is a template language the prototype has no other use for. All three are `every` + `if`, not `when` — an edge-triggered guard that one order cannot clear fires once and never re-arms, which is OP-17. Both stated consequences are now measured closed: M7's thermal challenge is solvable in template mode alone (68.4 °C peak and 100% throughput through a demand window, against 79.9 °C and 86% uncontrolled, with nothing typed), and a saturated buffer converts to capital instead of discarding it (+76.15 CR over 60 s). `thermal.test.ts`'s energy supply — the hand-written CCL this row names — is now generated from the template, and M7's pacing pins still pass. A test asserts every trade command is reachable from some template, so a good with a buy and no sell fails the build.                                                                                                                                                                                                                                                                                                                                                                            | High     | 🟢     |
| OP-21 | 2026-08-09 | **Resource readouts show absolute values but almost no rates, and no rate can be attributed to the process causing it.** Reported in play. `ResourcePool` already carries `ratePerSec` and the engine recomputes it every tick for compute, capital, energy and temperature (`src/core/engine.ts`), but `ResourcePanel` renders exactly one of the four — POWER DRAW — and only when it is negative (OP-19). So four live rates are computed and one is shown. Two distinct pieces of work sit here. **The cheap half:** surface the rates that already exist, signed, for compute, energy and core temperature. °C/s is the one that changes play the most — a player inside a demand window can read where the core _is_ but not whether they are winning, which is precisely the information a control problem needs. Note the compute rate as computed is daemon throughput only (`displayRate × netComputePerJob`); script fuel is drawn from the same pool and is not in it, so surfacing it unchanged would be a readout that disagrees with the meter next to it. **The expensive half, and the one actually asked for:** a net rate cannot distinguish a facility comfortably in balance from one where a −8/s drain is being masked by a scheduled process buying 100 energy whenever the reserve drops below a threshold. Same number, completely different situations — the second is one bad market print or one TERMINATE away from failure. Nothing in the sim attributes flow to its cause: `ratePerSec` is one scalar per pool, and while `run.telemetry` tracks per-process ops, compute and calls, it does not track capital or energy _moved_ by a process's commands. So this needs flow attribution — at minimum passive vs. script-driven, ideally per deployment, which would also hand OP-9 its capital-flow column for free. Design question: whether the split belongs in the resource panel, beside the responsible process in the monitor (see OP-25), or both. **2026-08-10 (M7.5 WP3): the cheap half is closed; the row stays open for the attribution half.** COMPUTE, ENERGY and CORE TEMP each carry a signed per-second rate on their label line, revealed with the rest of the system readouts (capital is deliberately not among them — credit flow over time is OP-29, and a rate there would be answering a different question badly). The row's own warning was the hard part and is dealt with rather than worked around: script fuel is now subtracted from the compute and energy rates, measured at the point the interpreter draws it and averaged over `BALANCE.readouts.rateWindowSec` — measured live at `-1.32/S` on the readout against a pool that moved `-1.350/S` over the next four seconds. Two things the row did not know: `temperature.ratePerSec` was measured from _after_ the step's heat had landed, so it could only ever report cooling and the °C/s the row calls the most valuable number was not merely unrendered but unavailable; and a raw per-tick derivative is unreadable anyway, because daemons land jobs in lumps — hence the trailing window (`/core/rates.ts`, not persisted). Measured across one thermal engagement: `+0.18°C/S` in the alert colour at 86.0 °C, then `-0.63` / `-2.87` / `-6.64°C/S` at 0.5 s, 2 s and 4 s after the coolant opened, with the core down to 67.0 °C. **Still open:** flow attribution — which process is responsible for a rate — which is the half the row says was actually asked for, and which stays in the post-prototype group with OP-9, OP-17 and OP-29.                 | Medium   | 🔵     |
| OP-22 | 2026-08-09 | **Install channels are a flat, unsorted list of twelve entries.** Reported in play as too messy to navigate. `UpgradePanel` renders `UPGRADES` in declaration order with no grouping, sorting or filtering, inside the 300 px side column (OP-24), and an exhausted channel keeps its full three-line entry and a dead `CHANNEL EXHAUSTED` button forever. By the thermal tier all twelve are listed and they span unrelated concerns — throughput (daemon, routing uplink, scheduler patch, queue buffer), capacity (memory grant, energy cell), script budgets (process table, op budget, iteration budget), power and cooling — with no visual separation, so choosing what to buy next means reading every entry every time. The data to group on does not exist: `UpgradeDef` has no category field, and grouping by `effect.kind` would produce twelve groups of one. Fix is a category on the content definitions plus sectioning in the panel. Design questions: what the categories are called in the system voice, whether an exhausted channel collapses or disappears entirely (disappearing loses the record of what was bought), and whether to sort by affordability — probably not, since that moves entries under the player's cursor as capital changes. **2026-08-10: one of those design questions is answered by the owner** — "completed install channels should be grouped separately and collapsed by default, to keep the list tidy and easy to browse". So an exhausted channel **collapses into its own section rather than disappearing**, which keeps the record of what was bought (the concern this row raised against deleting them) while costing one folded header instead of twelve dead three-line entries. Two questions stay open for WP5: the section names in the system voice, and whether the live channels are further grouped by concern or only split live/exhausted. Recorded as a note rather than a new row, because this row already named the question. See also **OP-28** — the list reveals entries three to four times faster than they can be bought, so grouping tidies a list that is separately too eager. **Closed 2026-08-10 (M7.5 WP5).** `UpgradeDef.category` added in `/content/upgrades.ts` with `UPGRADE_CATEGORIES` declaring the sections and their order; `UpgradeView` publishes it, and the panel sections on it rather than inferring anything. Both questions the row left for WP5 are answered: the live channels **are** grouped by concern as well as split from the exhausted ones, and the sections are `THROUGHPUT` / `CAPACITY` / `PROCESS EXECUTION` / `POWER AND COOLING`. Exhausted channels collapse into `CHANNELS EXHAUSTED // n`, folded by default, one line each, no dead button anywhere in the panel — worth 278.4 px of the side column on the twelve-channel list (1368.2 px with none exhausted against 1089.8 px with three folded). Sorting by affordability was not done, for the reason the row gives: it moves entries under the cursor. Five tests in `upgrades.test.ts` pin that the categories partition the catalogue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Medium   | 🟢     |
| OP-23 | 2026-08-09 | **Panels do not auto-collapse, so expanding one to read it leaves it expanded.** Requested in play: opening the execution log to check something, then having it fold again on a click elsewhere, would keep the screen tidy without a second deliberate click. OP-2 made every panel collapsible with persisted per-panel state (`src/ui/components/Panel.tsx`), which fixed "the page grows forever" but made tidiness a manual discipline — every expand costs a later collapse the player has to remember to make. Wanted as an option, not a default, and there are two traps. **It must not apply to panels the player works inside:** an editor that folds because the player clicked the market chart to check a price would be actively hostile, and the terminal, editor and template library are all in that class. **An outside click is the wrong trigger on its own** for panels that update: an execution log that folds while the player is waiting for the next entry defeats the purpose it was opened for. Likely shape is a per-panel auto-fold toggle, off by default, offered only on read-only readout panels, with today's persisted collapse state as the fallback. Interacts with OP-24 — if the side column gets wider, less of the tidiness problem remains.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Low      | 🔵     |
| OP-24 | 2026-08-09 | **The side column is a fixed 300 px while the terminal column absorbs every extra pixel.** Reported in play on a 16:9 desktop. `.screen-layout` is `grid-template-columns: minmax(0, 1fr) 300px` (`src/ui/terminal.css`) and nothing caps the shell width, so at 1920 px the left column runs to roughly 1540 px while the readouts stay in the same 300 px they get on a 1000 px screen. That is backwards on both sides: the side panels hold the content that actually wants width — install entries wrap to three lines (OP-22), profiler counters wrap, the market chart is squeezed — while the terminal column mostly holds prose and code that is _less_ readable at 1540 px than at 900. Cheap to change (the column definition, one breakpoint, and a check of the panels that assume a narrow column) but it is a judgement call rather than a defect, and the options differ: a proportional side column, a wider fixed one, a maximum width on the shell so the layout centres on very wide screens, or a two-column readout region above some breakpoint. Settle it with OP-22, since grouping changes how much width the install list needs, and re-check at 440 px where the layout is already single-column. **Closed 2026-08-10 (M7.5 WP5).** Settled as: a **share with a floor and a cap**, plus a cap on the shell. `grid-template-columns: minmax(0, 1fr) clamp(300px, 26%, 420px)` and `max-width: 1560px; margin-inline: auto` on `.terminal-screen`. At 1920 the terminal column goes 1556 → 1102.9 px and the side column 300 → 393.1 px, with the shell centred; the nine live install descriptions drop from 15 rendered lines to 11 and the market chart from 251 to 344.1 px wide. At 800 px the side column still sits on the 300 px floor, so nothing below a wide desktop changes, and 440 px is single-column as before.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Low      | 🟢     |
| OP-25 | 2026-08-09 | **PROCESS SCHEDULER and PROCESS PROFILER show overlapping information, and nothing says which answers which question.** Reported in play. Both panels are keyed on the same process names and both render the process's label, activations, total ops, total compute and a failure count (`ProcessMonitor.tsx`, `ProfilerPanel.tsx`) — five fields duplicated between two adjacent panels. The real differences are narrow and not visible from the titles: the monitor is **live state and control** (slots used, RAM footprint, last activation status and how long ago, TERMINATE), the profiler is **accumulated cost and diagnosis** (share of script compute, average ops, the calls/rejected/aborted split, and the plain-language report of GDD §6). They are divided by _when they were built_ rather than by purpose — the monitor was M4's debugging surface, the profiler is M5's, and the monitor was never revisited when the profiler landed. Two defensible resolutions: merge into one process panel with a live state row and an expandable cost/diagnosis section, or keep two and strip each to its own job (the monitor loses the accumulated counters, the profiler loses everything but cost and diagnosis). The choice is a prerequisite for OP-12, OP-13 and OP-14, which all add controls to whichever panel survives — deciding it afterwards means building those controls twice. Not merely tidiness: the prototype's answer to "debugging as gameplay" currently opens by asking the player which of two similar tables to read. **2026-08-10 (M7.5 WP4a): merged.** One **PROCESS TABLE**, one row per process, split by question rather than by milestone — live state always visible, accumulated cost and the GDD §6 report behind a per-row `COST DETAIL` disclosure gated on the instrumentation unlock exactly as the profiler panel was. The five duplicated fields are each rendered once; the two snapshot sub-trees are joined on `ProcessView.profileKey`, published by the engine so the UI does not re-derive the convention. Merge was chosen over "keep two and strip each" because WP4b's controls all act on a process: under two panels a player would rename one in the scheduler and read its counters in the profiler. No save-shape change. Verified at 1280 px and 440 px; three tests pin the join.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Medium   | 🟢     |

| OP-26 | 2026-08-09 | **No template exists until 320 cleared requests, so the scripting tier opens typing-only.** Found while writing WP1b's last directive, which originally offered the template library as the no-typing route to a first process. It is not there: the scripting interface is released at 200 lifetime requests (`BALANCE.ccl.unlockAtJobs`), while the earliest template — RESERVE GUARD — is `requires: 'conditions'` and the conditions tier lands at 320 (`/content/templates.ts`, `TemplateLibrary.tsx` filters on `constructs[def.requires]`). For 120 cleared requests, roughly the length of the whole opening stretch again, the editor is present and the only way to put anything in it is to type. That is precisely the player GDD §25 exists for and the tester OP-6 is waiting on, and it sits on the coverage rule §25 gained on 2026-08-09 ("a designed solution must be expressible in templates alone") — the interface is not closed under the actions the game asks for at the moment it first asks for them. The directive no longer promises what is not there (it points at the API reference and `process_job()`, which are), so this is not blocking WP1b, but it is unresolved for a template-only player. Two candidate fixes: a tier-0 template that generates a bare command sequence (content-only, one entry in `/content/templates.ts` needing a `TemplateTier` that is always true), or moving the conditions grant closer to the editor's. Natural companion to **WP2**, which is already opening `/content/templates.ts` for OP-20 and OP-4. **2026-08-09 (M7.5 WP2): weighed and left open — the stated cheap fix is wrong.** A tier-0 template is **not** content-only. A bare command sequence is only worth offering if its length is a parameter, and `renderTemplate` does `{{param}}` substitution — it cannot repeat a line _n_ times, so a count needs repetition machinery in the renderer (a `{{repeat}}` block or equivalent); without a count it is a fixed-length snippet, which is a worse first template than none. The row's other candidate, moving the conditions grant from 320 nearer the editor's 200, is a pacing change to the whole tier ladder and belongs to M8's balance pass. Both remain viable; neither is free, so the choice is an owner decision rather than something to fold into a package scoped to two other rows. WP2 did close the trade half of §25's coverage rule, which narrows this row to exactly one claim: **template mode is empty for the 120 requests between the editor's release and the conditions tier.** | Medium | 🔵 |
| OP-27 | 2026-08-10 | **The system terminal forces itself back to the newest line ten times a second, so it cannot be scrolled up.** Reported in play: "i cannot scroll up without it scrolling down by itself." Not a scroll-anchoring quirk — it is an unconditional jump. `TerminalPanel.tsx` runs `useEffect(toBottom, [terminal])` where `toBottom` sets `scrollTop = scrollHeight`, and the dependency is the snapshot's `terminal` array, which `engine.getSnapshot()` rebuilds as `[...run.terminal]` on **every revision**. `markDirty()` bumps the revision on every `tick()` that runs at least one sim step, and `startGameLoop` calls `sync(engine)` each rAF frame, so at the 10 Hz timestep the store swaps in a new snapshot — and therefore a new `terminal` identity — ten times a second whether or not a line was written. Measured: one `tick(100)` with no click leaves the line count at 60 → 60 while `a.terminal === b.terminal` is **false** and the revision advances. So the effect re-runs 10×/s and the scroll position is unrecoverable while the sim is running. Two things are wrong and both want fixing: the effect should only stick to the bottom when the player is **already** at (or near) the bottom, which is the conventional log-view behaviour and is what makes reading back possible; and the dependency should be something that changes when output changes — the newest line's `id`, or `run.nextTerminalId` — rather than an array rebuilt every frame. Worth checking the execution log and research feed for the same pattern. **Severity is High because the terminal is the game's only narrative and command-output surface**: a player who cannot scroll back cannot read what a script printed, which is the GDD §6 debugging loop, and cannot re-read a diegetic beat they scrolled past. Routed to **WP3** — a readout that cannot be read is the same family as a readout that does not tell the truth, and it is a small defect fix rather than new scope. **2026-08-10 (M7.5 WP3): closed.** Both halves done as the row specifies: the effect is keyed on the newest line's `id`, so it runs when output changes and not when the store swaps in an identical array; and it sticks to the bottom only for a reader who is already within a line of it. A reader who scrolls back gets a floating control naming how many lines have arrived since (`12 NEW LINES BELOW ↓`), and re-attaches by clicking it or by scrolling back down. Measured: scrolled back 300 px, the same line stayed at the top of the viewport across **60 consecutive snapshot revisions** (6 s of sim); before the fix the effect fired on every one of them. Following is intact — at the bottom new output still sticks, and the control restores that state. The execution log and research feed were checked for the same pattern and do not have it: both render newest-first and neither auto-scrolls. One limit found and logged separately as **OP-32**: scroll-back reaches 60 lines, because the in-memory terminal is the same ring as the persisted tail. | High | 🟢 |
| OP-28 | 2026-08-10 | **Install channels are revealed roughly three to four times faster than they can be afforded, so the early game lists options the player cannot act on and reveals them in an order that does not follow need.** Reported in play: "i unlock Inference daemon before ever having to upgrade batch aggregation window", and "i also got request routing uplink before installing an inference daemon — all too fast and close to one another. I didn't get a chance to figure out what the first option does coz i didn't even unlock it yet." Measured on a genuine cold start, played optimally (trigger pressed whenever anything is queued) and **without spending anything**, which is the most generous case: BATCH AGGREGATION WINDOW revealed at **13.5 s / 15 requests** but affordable at **46.3 s / 48 requests**; INFERENCE DAEMON revealed at **30.2 s / 30** — i.e. 16 s _before the first channel is even affordable_ — and affordable at **91.3 s / 120**; REQUEST ROUTING UPLINK revealed at **53.8 s / 60**, 37 s before the daemon is affordable, while the player still owns nothing; AUXILIARY POWER FEED revealed at **72.5 s / 90**, also before the daemon. By the time a first install is possible, two channels are listed; by the time the daemon is possible, all four are. The cause is structural, not a wrong constant: **reveal is keyed to `unlockAtJobs` (lifetime requests) while affordability is keyed to accumulated capital at `capitalPerJob` = 0.25 CR**, so a channel costing _C_ needs _4C_ requests of income but is revealed at a threshold far below that — 3.2×, 4×, 3× and 2.2× ahead respectively. Buying anything makes it worse, since spending resets the climb. Two candidate fixes, and they are different games: key the reveal to _affordability_ (reveal at, say, 60% of cost) so a listed channel is always nearly buyable; or keep job-count reveals and re-space `unlockAtJobs` against the real cost curve. Interacts with **OP-22** (the list is also ungrouped) and with the WP1b directive, which already tracks credit against the price the channel is asking. Routed to **M8 task 5 (full balance pass)** — the fix is balance numbers and the whole ladder has to move together — but flagged for **WP6**, since the first non-programmer sitting will hit this in its first ninety seconds and their feedback should be read knowing it. | Medium | 🔵 |
| OP-29 | 2026-08-10 | **There is no way to see credit flow over time, so a player cannot tell whether the algorithm they just deployed is better or worse than the one it replaced.** Requested in play: "I need a better way to visualize my credit flow. Maybe an expanded view that shows me stats per duration like current rate up or down; it is sometimes difficult to gauge if current algorithm used is better or worse than the previous one." This is adjacent to **OP-21** but is not the same ask, and neither half of OP-21 supplies it: OP-21's cheap half surfaces the _instantaneous_ signed rate, and its expensive half attributes that rate to the process causing it — but "is this version better than the last one" is a question about **two intervals separated by an edit**, which nothing in the sim records. The pieces that exist: `run.telemetry` holds per-process cumulative ops/compute/calls, `MarketState` holds lifetime `earned`/`spent` plus `netAtRegimeStart` (the one existing example of "measured since a moment"), and the execution log is a 120-entry ring buffer with timestamps. What is missing is a windowed series — capital per minute over the last _n_ minutes — and a marker for when a deployment changed, so the two can be compared. Cheapest honest version is probably a small capital-flow sparkline with a rolling rate, plus the redeploy events marked on it; the fuller version is per-deployment capital attribution, which is OP-21's expensive half and would also hand **OP-9** its capital-flow column. **A delivery idea came with it, worth recording separately because it is a UI pattern rather than a readout:** Paradox-style hover detail — on hover a small popup shows the critical breakdown, and holding the hover past ~1 s locks the popup so its contents can be interacted with. That would suit the resource panel, the profiler and the install channels equally, and would relieve the width pressure behind **OP-24** and **OP-22** by moving detail out of the always-visible column. Routed to the **post-prototype group with OP-9, OP-17 and OP-21's attribution half** — the plan already names those three as one coherent piece, and per-deployment flow is the plumbing all four need. The hover-popup pattern is separable and could be prototyped earlier on one panel if WP3 wants it. | Medium | 🔵 |
| OP-30 | 2026-08-10 | **Idea, not a defect: a post-M8 milestone of replay modifiers, so a second run differs in kind and not only in speed.** Proposed by the owner: "Once M8 is done, do a final M9, adding some replay modifications; i.e. upon new game +, ram shortage affects all run and you have reduced capacity and increased costs on upgrades, etc." Distinct from what M8 already carries: M8's prestige fork spends architecture points on **permanent modifiers that make run 2 easier**, and reseeds the world so it is _different_; this proposes modifiers that make a run **harder or differently shaped** — a standing RAM shortage, raised install costs — which is a difficulty/variety axis the design does not currently have anywhere. GDD §22 covers prestige and §31 fixes prototype scope, so adopting this is a **scope change needing a GDD amendment before it becomes a milestone**, not something to fold into M8. Worth deciding only after the M8 verdict, since "is a second run worth playing?" is one of the questions that verdict answers, and the answer changes whether this is a milestone or a backlog line. Recorded here so it is picked up rather than lost in chat. Routed to **post-M8 / candidate M9**; also listed in the Backlog. | Low | 🔵 |
| OP-31 | 2026-08-10 | **Expanding the left column's panels pushes content past the bottom of the shell where nothing can scroll to it, and crushes the terminal to zero height.** Reported in play: "if i expand everything in the main panel, it flows off screen and i can't scroll down to whatever goes offscreen." Confirmed and measured. `.terminal-screen` is `height: 100%; overflow: hidden` (the fixed-viewport desktop shell, `src/ui/terminal.css`), `.side-column` carries `overflow-y: auto` — but `.terminal-column` carries **no scroller at all**, so its overflow is clipped by the shell with no way to reach it. The document cannot take over either: at desktop widths `.terminal-screen` is a locked-height flex shell, so `document.scrollHeight === clientHeight`. Measured at **1280 × 800** with the directive, terminal, trigger, editor and execution log expanded: the EXECUTION LOG's bottom sits at 739 px against a column ending at 732 px — 7 px clipped, page scroll range 0. At **1280 × 720**, the same layout clips the EXECUTION LOG by **87 px** with page scroll range still 0, and because `.terminal-panel` is the only `flex: 1` child with `min-height: 0`, the squeeze lands entirely on it: `.terminal-output` measured **0 px tall while holding 61 lines** — the SYSTEM TERMINAL is fully expanded and completely invisible. This is the desktop mirror of the 2026-08-06 narrow-viewport fix, which released the document scroll below 760 px and left the desktop shell alone; OP-2 then made every panel collapsible, which made it easy to have five expanded panels in one column. Candidate fixes: give `.terminal-column` its own `overflow-y: auto` (matching the side column), or drop the locked-height shell at desktop widths as the ≤760 px path already does, or set a floor on `.terminal-panel` so the terminal cannot be squeezed below a few lines. The last one is worth doing regardless — a panel the player has explicitly expanded should never render at zero height. Routed to **WP5**, which already owns the shell's geometry through OP-24 and OP-22 and edits the same file. **If WP5 is cut to a follow-up (see "The cut line"), this row must be pulled out and done anyway** — it is a High-severity defect sitting in the package most likely to be dropped. **Closed 2026-08-10 (M7.5 WP5).** Two of the three candidate fixes, both of them: `.terminal-column` gets its own `overflow-y: auto` (matching the side column), and `.terminal-panel:not(.panel-collapsed)` gets a `min-height: 10rem` floor, because an expanded panel rendering at zero height is wrong however much else is on screen. Measured at 1280 × 720 both ways in one session — pre-fix the output is **27.5 px tall holding 50 lines** with the column scroll range at **0**; after, the terminal holds 160 px with a 103 px output, and the execution log 75.5 px below the fold is reached by scrolling the column **76 px**. The locked-height desktop shell is kept, so `document` scroll range stays 0 at desktop widths and the ≤760 px document-scroll path is untouched.| High | 🟢 |
| OP-32 | 2026-08-10 | **Scroll-back in the system terminal reaches only 60 lines, because the in-memory buffer and the persisted save tail are the same ring.** Found while verifying OP-27: with the terminal held at a reading position, 30 new lines evicted the line being read out from under the reader (measured — `scrollTop` pinned to 0 and the top-of-viewport line changed). `pushTerminal` splices `run.terminal` down to `BALANCE.save.terminalTailLines` (60) on every write, so the cap that exists to keep the save file small is also the cap on how far a player can read back. WP3 fixed the forced jump, which was the defect; this is the ceiling underneath it. It matters for the same reason OP-27 did — GDD §6 makes reading a script's output part of debugging, and a chatty `every 1 seconds` process can push a printed result out of reach in under a minute. Not a one-line change: raising the buffer without raising the persisted tail means splitting the two (the in-memory ring keeping N, the save keeping 60), which touches the save shape; raising both feeds directly into **OP-8** (the save is already ~44 kB and the terminal tail is 4.5 kB of it). Routed to **Open items**, not folded into WP3 — the milestone rule is that a new item found during M7.5 waits for M8 unless it blocks a package, and WP3's own acceptance (a player can read the terminal back) is met at 60 lines. Natural companion to OP-8, which will be opening the same serialization. | Medium | 🔵 |

## Playtest findings log

_(append dated entries here as playtests happen)_

**2026-08-08 — first-contact feedback, one new player.** Reported: it was not clear what they were supposed to do, and they said a tutorial would be needed. No further detail was captured — in particular it is **not** recorded whether this tester was a non-programmer or whether they used template mode, so this does **not** satisfy the non-programmer playtest committed to in the Risks table (OP-6 stays open and the M6 task stays 🔴).

Logged as OP-15. The finding is upstream of everything the prototype exists to test: the CCL editor does not appear until 200 lifetime processed jobs, so a player who disengages on the opening screen never reaches the programming layer the exit question is about. Worth treating as a blocker on any further playtesting, since every subsequent tester would be expected to hit the same wall first and their feedback beyond that point would be hard to obtain.

**2026-08-10 — owner play on the deployed build, first sitting after WP1a/WP1b/WP2 went live.** The three M7.5 packages were committed but not pushed until this date, so the first pass of this sitting was against `54ce369` — a build with no directive, no boot briefing and no INBOUND meter — and the "still no guidance for a new player" observation from it is not evidence about WP1b, which was not in that build. `74f0218` is now deployed and verified serving (asset hash matched), and onboarding is to be re-judged from a cold start on it; note that an existing save posts no directive by design, so re-testing requires PURGE. Five findings survive independently of which build they were made on and are logged as **OP-27** (the terminal forces itself to the newest line ten times a second, so it cannot be read back), **OP-28** (install channels are revealed three to four times faster than they can be afforded, and in an order that does not follow need — measured on a cold start), **OP-29** (no way to see credit flow over time, so one algorithm cannot be compared with the one it replaced; a Paradox-style hover-detail pattern proposed as the delivery), **OP-30** (an idea for a post-M8 milestone of replay modifiers) and **OP-31** (expanding the left column's panels clips them where nothing can scroll to them, and crushes the terminal to zero height). A sixth finding — exhausted install channels should be grouped separately and collapsed by default — answers a design question **OP-22** already posed and is recorded as a note inside that row rather than as a new one. **This does not close OP-6:** the tester is the owner, not an external non-programmer.

## Change log

| Date       | Change                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-06 | v1.0 — initial plan created.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-06 | M0 complete: scaffolding, tooling, terminal skeleton, GitHub Pages workflow (publish awaits a remote — owner action).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-06 | M1 complete: engine facade, 10 Hz fixed timestep, seeded PRNG, compute/capital + inert placeholder resources, job queue + EXECUTE with content-stepped acceleration curves, SaveFileV1 with autosave/export/import, narrative feed. 22 tests incl. seed-42 deterministic replay. Acceptance verified in-browser: click loop, staged readout reveals, refresh restores exact state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-06 | GitHub repo created (`aevisisawesome/breakout-game`, public) and Pages enabled; deploy verified live. M0 deploy task closed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-06 | M2 complete: 6 content-defined upgrades ("install channels"), inference daemons with compute overhead + energy drain/throttle, click overclock buff, RAM footprints + memory grants, SaveFileV2 with v1→v2 migration, `advanceOffline` coarse catch-up (8 h cap). 45 tests incl. pacing pins (first daemon ≤ 4 min of manual play; automation beats manual-only by 10 min). Verified in-browser: install flow, idle daemon processing, overclock readout, offline catch-up on reload.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-06 | Fix (reported from iPhone 16 Pro Max / Safari): the ≤760 px single-column fallback locked ~1800 px of stacked panels into one viewport, so the side column painted over the CCL editor and API reference. Narrow screens now scroll as an ordinary document; desktop layout unchanged. Verified by geometry + hit-testing at 456 px and 375 px wide and at 1280×720. Not the deferred mobile layout (GDD §31) — fallback usability only.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-06 | M3 complete: CCL v0 end to end — hand-written lexer/parser/tree-walking interpreter in `/src/ccl`, fuel drawn from compute per op-unit with budget + pool-exhaustion aborts, API registry (`stats.cash/compute_available/jobs_waiting/energy/temperature`; `print`, `process_job`, `buy_compute`), CodeMirror 6 editor panel with CCL highlighting/live diagnostics/unlock-gated autocomplete/API reference, script-access unlock at 200 lifetime jobs with its narrative beat, SaveFileV3 (editor buffer persisted, v2→v3 migration). 100 tests (55 new: lexer/parser goldens, interpreter semantics + fuel accounting, registry 1:1 coverage, engine scripting flow incl. determinism replay). Acceptance verified in-browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-07 | Housekeeping (no gameplay change): repo-wide `npm run format` pass clearing Prettier drift in 9 files, including all four `Docs/*.md` (never previously formatted); `format:check` + `lint` added as blocking gates in the deploy workflow ahead of `build`; `CLAUDE.md` added at the repo root. The Docs diff was verified rendering-neutral — before/after HTML is byte-identical for every table in all four documents, the only rendered change being three embedded TS code fences in the TDD. `npm test` (100 passing), `lint`, `format:check` and `build` all green.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-08 | M4 complete: CCL tiers 3–4 end to end. Grammar gains `if`/`else if`/`else` and `and`/`or`/`not` (short-circuiting, no truthiness) plus top-level `every N seconds\|ticks` and `when expr` declarations; the grammar is unlock-gated at parse time and the editor's linter and autocomplete read the same flags. The scheduler runs due processes in slot order inside `tick()`, sampling `when` guards at 2 Hz with edge-triggering; deployments cost one slot per declaration and RAM by AST size, are saved as source text and recompiled on load. Process monitor panel, template mode v0 (3 templates generating visible CCL), conditions/scheduler unlock beats at 320/480 lifetime jobs with their narrative entries plus a first-deploy audit beat, PROCESS TABLE EXTENSION install for extra slots, bounded offline execution of `every` processes, SaveFileV4 with v3→v4 migration. 164 tests (64 new). Also fixed a narrow-screen layout bug found during verification: a long CCL line scrolled the whole page sideways at 440 px. Acceptance verified in-browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-08 | M5 complete: CCL tier 6 plus the debugging layer. `for i in range(n)` parses with a literal repeat count checked at parse time against a derived iteration limit (10, ×10 with ITERATION BUDGET EXTENSION); the op budget becomes derived too (+300 per EXECUTION BUDGET EXTENSION, max 3) so raising the limit is a sequence to learn rather than a trap. New persisted execution log (120-entry ring buffer) records every RUN and scheduled activation, and a guard only when it changes into an abnormal state. New profiler panel gives per-process activations, avg/total ops, compute share and rejections, each with a plain-language failure report (GDD §6) chosen in `/core/diagnostics.ts` from wording in `/content/diagnosis.ts`. Instrumentation unlocks at 620 lifetime jobs, loops at 760, each with its narrative beat; a fourth template (BATCH DRAIN) covers the new tier. SaveFileV5 with v4→v5 migration. 201 tests (37 new, incl. runaway-loop safety at both the parse and fuel boundaries). Acceptance verified in-browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-08 | Added an "Open items from prototyping" section (OP-1…OP-7) for defects, limitations and deliberate shortcuts that are not scoped to a milestone, plus a risk row pointing at it. Seeded from M5 verification and from three items raised in play: the stale `LAST RUN // FAULT` after a successful DEPLOY, panels not being collapsible, and job supply being the binding constraint at the loop tier. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-08 | M6 complete: the market layer end to end. Two tradable goods (compute, energy) priced as `base × seasonal × regime × noise`, where cycles carry a skew so rise and fall differ and noise is a bounded random walk off the run's PRNG; the market keeps its own clock so offline catch-up cannot freeze prices. `market.price(good)` and `market.average(good, n)` arrive as CCL **namespaced calls** over a 300-sample ring buffer, alongside `sell_compute`/`buy_energy`/`sell_energy` and market-priced `buy_compute`, all with a flat fee and per-order slippage. Bindings are now individually unlock-gated, so the exchange is a diegetic reveal and a locked name explains its tier. Energy became a real constraint: the draw scales with daemon throughput and interpreter ops, and at full build-out outruns every power feed. New market terminal with a hand-rolled SVG price chart and manual orders; two new templates (SPREAD TRADER = the GDD §7 algorithm, TREND-FILTERED TRADER = the adaptation); scripted STABLE→HIGH_VOLATILITY shift at 900 s of exchange time with an advisory line and audit entry that never name the regime; SaveFileV6 with v5→v6 migration. 243 tests (42 new), including the acceptance scenario across three seeds. Also took OP-1 (stale `LAST RUN // FAULT` after a successful DEPLOY → the status line now reports the last _action_, labelled, and every success clears it) and OP-3 (REQUEST BUFFER EXPANSION makes queue depth an upgradeable derived stat).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-08 | Balance/behaviour changes worth calling out, all recorded in the TDD Decision Log: the regime shift **lengthens the cycle** rather than only raising amplitude (raising volatility alone makes buy-low/sell-high _more_ profitable, which was the first tuning attempt and the opposite of the design intent); `buy_compute` is market-priced at a 0.5 CR base, up from a fixed 0.2; daemon energy draw scales with throughput, so DAEMON SCHEDULER PATCH now costs power as well as capital. ESLint gained the missing `/content` → `/core`,`/ccl` import ban, closing a hole in the §3 layering that CLAUDE.md already claimed was enforced.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-08 | Found in M6 verification and fixed the same session: the post-shift narrative beat was written as a job-count entry and fired in the same instant as the shift advisory, describing a failure the player had not yet experienced. It is now gated on a measured drawdown since the shift (`market-loss` flag, `netAtRegimeStart` on the market state). New open items OP-8 (save file grew to ~44 kB), OP-9 (a strategic failure produces no profiler diagnosis — the class of failure M6 exists to teach), OP-10 (`market.average` is priced flat regardless of window). OP-6 moved to 🟡: the non-programmer playtest is now a tracked 🔴 task rather than mitigation prose, but still needs an external tester.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-08 | OP-2 closed: every panel is a collapsible section (`src/ui/components/Panel.tsx`) whose header is the toggle, with per-panel collapse state persisted in `localStorage` (presentation, not game state, so it stays out of the sim). The body is hidden rather than unmounted so folding a panel cannot destroy the CodeMirror instance or lose the terminal's scroll position. Measured at 440 px: the page is 1939 px tall with everything expanded and 900 px — exactly one viewport — with everything folded. Two defects found and fixed while building it: the per-panel `display: flex` rules overrode the `[hidden]` attribute's `display: none`, so a "collapsed" panel stayed fully visible; and re-expanding the terminal left it scrolled away from the newest line, because a hidden scroller has no height to preserve a position in. No gameplay change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-08 | Added OP-11 from review: the CCL API reference names parameters (`market.price(good)`, `print(value)`) without stating what may go in them, and the actual domains live only in `/core/registry.ts` misuse messages — so they are learnable only by triggering a fault. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-08 | Added OP-12 and OP-13 from play: deployed processes cannot be named (`PROC-nn` comes from a never-reset counter and is the join key across the monitor, execution log and profiler), and a deployed process can only be terminated, not pulled back into the editor for editing. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-08 | Added OP-14 from play: a running process can only be terminated, never paused, and TERMINATE is destructive — it takes the profiler row and all of its counters with it. That leaves no way to isolate one process while debugging, and no calm response to M6's own regime shift when a deployed trader starts losing capital. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-08 | First playtest finding recorded in the (previously empty) Playtest findings log: a new player did not know what they were supposed to do and asked for a tutorial. Added OP-15 (High) — the opening screen states no goal and no first action, the only instruction is diegetic flavour, a click in the first second returns an error because the queue has not filled yet, and the GDD has no onboarding section at all to implement against. Does not close OP-6: it is not recorded whether the tester was a non-programmer. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 2026-08-09 | M7 complete: temperature is real. The core is a lumped mass relaxing towards ambient (`dT/dt = heatRate − dissipation × (T − ambient)`, `/core/thermal.ts`), warmed by **work done** — every processed request, whoever triggered it, and every interpreter op-unit — so a wasteful loop is now physically costly and not merely slow. Above a soft threshold daemon throughput degrades linearly, which is a negative feedback that makes most loads settle; above a hard threshold a latching **watchdog with hysteresis** suspends daemons, deployed processes, queued RUNs and the manual trigger until the core is back in band. The model runs from tick 0 — it is physics, not a tier — so the readout climbs with the build-out long before the controls arrive. The **thermal tier at 1300 lifetime jobs** grants `reduce_clock_speed()` / `boost_cooling()`, the `stats.temperature_limit` read, the COOLANT LOOP EXPANSION install, a THERMAL GOVERNOR panel with manual levers, and the recurring **priority demand windows** that are the challenge. Both actuators are sustained holds refreshed by repeat calls; `boost_cooling()` charges a spin-up only when engaging from idle, which is what makes an oscillating controller cost more than a latched one. New THERMAL GOVERNOR template, two narrative beats (tier grant, and a watchdog-trip beat gated on the trip actually happening), SaveFileV7 with v6→v7 migration. 285 tests (35 new), including the pacing pins that define the challenge: a maxed build-out rests below the soft threshold in calm conditions, exceeds the hard threshold once a window opens, and survives on either lever alone.                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-09 | Balance/behaviour notes worth calling out, all recorded in the TDD Decision Log: the coolant hold is short (4 s) relative to how long the core takes to reheat, so a controller has to _keep_ asking — that is the whole mechanism behind the oscillation lesson, and lengthening it flattens the difference between a good controller and a bad one to nothing (measured). Full COOLANT LOOP EXPANSION protects a daemon-only build-out through a window but not one also running scripts, so cooling is bought headroom rather than an exemption. An offline return hands the core back at the _resume_ threshold rather than the trip point: offline throughput is already capped at what the cooling supports, so returning a player to an instant shutdown would punish the absence, not a decision.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-09 | Took two open items alongside M7. **OP-11 closed**: every documented CCL parameter now carries a stated domain, composed from the same balance numbers the validators enforce and rendered in both the reference panel and autocomplete, with a test pinning parameters against signatures in both directions — the domains are no longer learnable only by triggering a fault. **OP-7 closed**: the dev handle gained `backup()` / `restore()` / `persist()` / `dump()`, neither helper touching `localStorage` directly, so a forced-state playtest can no longer eat the save; exercised end to end in this session's verification. New open items OP-16 (the idle temperature readout no longer flickers — a deliberate trade, but the opening screen has no moving elements to spare, see OP-15), OP-17 (a `when` guard that fires once and never re-arms produces no profiler diagnosis — same family as OP-9), OP-18 (clock ticks are fractional after an offline catch-up).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-09 | **M7.5 WP1 split into WP1a (opening screen is not dead) and WP1b (the briefing)**, seven packages now rather than six. Split by _design dependency_, not by phase: WP1a is everything true whatever the briefing says (the first-click `NO REQUESTS QUEUED` fault, OP-16's missing motion), WP1b is the design decision, the copy and the implementation. A design-session/build-session split was considered and rejected — onboarding is hard to judge on paper, and an amendment describing a design that implementation then disproves is permanent, since the Amendment Log forbids rewriting history. The boundary also gives the briefing's tone an owner review before it is built (§33.3) and makes WP1b cheap to redo, which first-contact copy normally needs. WP1a additionally establishes the cold-start verification routine WP1b depends on — onboarding is the first thing in this project that cannot be checked from a built-out save. GDD §34's forward references updated to WP1b; the session prompt gained the WP1b-after-WP1a constraint and the cold-start verification step. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-09 | **Milestone M7.5 — legibility and revision pass — inserted between M7 and M8**, with six work packages built from open items already logged below (WP1 first contact, WP2 template parity, WP3 readouts, WP4a process-panel decision, WP4b process lifecycle, WP5 side column, WP6 first non-programmer sitting) plus the ordering constraints, the cut line, the list of items deliberately left out, and the per-session prompt the milestone is worked with. Rationale: M8 produces the prototype verdict out of an external playtest and one full run, and these items corrupt that measurement rather than merely annoying — fixing them afterwards recovers nothing. Numbered `.5` rather than renumbering M8, which is referenced by name from TDD §7 and from both logs. Companion doc changes in the same session: **TDD** §8 records the planned v8 (M7.5) and v9 (M8) save-shape changes so they cost two migrations rather than four, §9 records the three known-wrong parts of the UI table as scheduled rather than accepted, and three Decision Log entries cover the milestone's insertion, the OP-25-before-OP-12/13/14 constraint, and the migration budget; **GDD** gains §34 (First contact and onboarding — what it must achieve, the constraints, the open questions, with the design left to WP1) and a coverage rule in §25 that template mode must be closed under the actions the campaign requires, both with Amendment Log entries at v1.3. `Docs/README.md` unchanged — no document was added. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-09 | Added OP-21…OP-25 from play, all five reported as friction rather than faults. OP-21: readouts show levels but not rates, and a net rate cannot separate a facility in balance from one whose drain is being masked by a scheduled top-up — the display half is nearly free (`ratePerSec` is already computed each tick for four pools and only one is rendered), the attribution half is new work that would also serve OP-9. OP-22: twelve install channels in one flat unsorted list, exhausted entries included. OP-23: panels stay expanded until manually folded; wanted as an opt-in auto-fold, with the editor and terminal excluded. OP-24: the side column is a fixed 300 px while the terminal column takes every extra pixel, so the panels that want width do not get it. OP-25: the process scheduler and the process profiler duplicate five fields and are divided by which milestone built them rather than by what they answer — settling that is a prerequisite for OP-12/13/14, which all add controls to whichever panel survives. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-09 | Added OP-19 and OP-20 from play. OP-19: the POWER DRAW row is conditionally rendered, so at an empty energy reserve it mounts and unmounts with the accepted full/throttled oscillation and resizes the whole side column ten times a second — the sim is behaving as designed, the readout is not. OP-20: template mode can buy compute but cannot buy energy or sell anything, which leaves M7's thermal governor unusable without hand-written code and leaves a saturated pool discarding capital that a template-only player cannot act on. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-09 | **M7.5 WP1a complete — the opening screen is not dead.** Two changes, both small and both first-contact. (1) A run now opens with `BALANCE.jobs.initialQueued` = 3 requests already waiting, so the first press of the trigger processes work instead of returning `NO REQUESTS QUEUED` — the queue used to start empty and fill at 0.9/s, which made an error message the player's first feedback (**OP-15**, partial: the design gap it names is WP1b's). An empty-queue press is no longer a fault either: it prints `NO REQUESTS QUEUED // NEXT INBOUND IN {seconds}S` in the system voice, because being ahead of the inbound rate is a state of the world, not a failure. (2) **OP-16 closed** with an INBOUND meter in the trigger panel — rate, countdown and a bar filling towards the next request off the sim's own fractional arrival accumulator. Deliberately _not_ done by restoring the pre-M7 temperature flicker: that readout is gated behind the 10-job system-readout unlock, so it was never on the opening screen, and adding noise to a number the thermal challenge now asks the player to control would be a regression. No save-shape change (v7 stands; `initialQueued` only touches `newRunState`, so existing saves are untouched). 287 tests. Verified from a genuine cold start, the routine WP1b depends on.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-09 | **M7.5 WP1b complete — the briefing (OP-15 closed).** Onboarding is a **standing operator directive**: a panel above the terminal posting one objective at a time — the action, what it produces, what completing it releases, and a counter towards the threshold it quotes — plus three briefing lines at boot saying what the node is, what a cleared request returns, and that capability is released against measured throughput, automation before the scripting interface. Five directives, retiring at the first RUN with a closing line; each looks exactly one release ahead, so tiers 3–6 keep their reveals. GDD §34's three open questions are answered in the GDD (form, skippability, look-ahead) and its outstanding requirements marked met; the design was committed there in the same session it was built, which is the reason WP1 was split. **No save-shape change:** the directive is derived each snapshot from state the run already holds, so v7 stands, M7.5's single v8 migration stays with WP4b, and every existing save resolves past the set and posts nothing. Skipping is folding the panel — the existing per-panel `localStorage` preference, outside the save. One narrow-screen layout change: while a directive is posted, the ≤760 px column hoists the inference trigger above the terminal, because a 257 px directive panel put the button it names below the fold. 297 tests (10 new). Verified from a genuine cold start at 1280 px and 440 px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-09 | Added **OP-26** from WP1b verification: the scripting interface is released at 200 cleared requests but the earliest template requires the conditions tier at 320, so for 120 requests the editor can only be filled by typing — an accessibility gap on GDD §25's own coverage rule, and on the path of the non-programmer tester OP-6 is waiting for. Found because WP1b's last directive originally promised the template library; the copy was corrected in the same session to point at the API reference and `process_job()`, which do exist at that point. Candidate fixes are content-only and belong with WP2, which already opens the same file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-09 | **M7.5 WP2 complete — template parity (OP-20 and OP-4 closed).** Three new templates, all content: **RESERVE TOP-UP** (buy energy against a floor) and **COMPUTE SURPLUS SELL** / **ENERGY SURPLUS SELL** (sell a pool back while it sits above a fraction of its own capacity). Template mode is now closed under the trades the campaign asks for — every one of `buy_compute`, `sell_compute`, `buy_energy`, `sell_energy` is reachable without typing, and a test makes a missing counterpart a build failure rather than a playtest finding. Both of OP-20's stated consequences are measured closed: M7's thermal challenge is now solvable in template mode alone (68.4 °C peak, 100% throughput, 0 watchdog trips through a demand window, against 79.9 °C and 86% uncontrolled), and a saturated buffer becomes capital instead of discarded surplus. `thermal.test.ts` no longer carries the hand-written energy supply the OP-20 row cites — it is generated from the new template, and M7's pacing pins still pass. **OP-4** closed by giving a template parameter a live ceiling: `maxFrom` names a derived stat, and `paramMax`/`clampParam`/`renderTemplate` take a `TemplateLimits` argument the caller reads from the snapshot, so `/core/templates.ts` stays pure. BATCH DRAIN's repeat control moves from `MAX 10` to `MAX 100` the instant ITERATION BUDGET EXTENSION lands. No save-shape change (v7 stands; M7.5's single v8 migration stays with WP4b). 315 tests (18 new). Verified in-browser at 1280 px and 440 px.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-09 | **OP-26 weighed during WP2 and deliberately left open, with its row corrected.** Its recorded cheap fix — "a tier-0 template … content-only, one entry in `/content/templates.ts`" — is wrong: `renderTemplate` substitutes placeholders and cannot repeat a line, so a bare command sequence with a length parameter needs repetition machinery in the renderer, and without a length it is a fixed snippet worse than no template. The alternative in the row (move the conditions grant nearer the editor's) is a tier-ladder pacing change that belongs to M8's balance pass. Neither is admitted to a package scoped to OP-20 and OP-4; the row now states the corrected cost and is narrowed to its one surviving claim. GDD §25 records the same gap in the section that carries the coverage rule, so the rule stays honest about where it is unmet. Docs-only change for this item; no code.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 2026-08-09 | **M7.5 WP1a, WP1b and WP2 pushed to `main` and deployed.** The three packages had been committed but held unpushed, so the public playtest build was still `54ce369` — no directive, no boot briefing, no INBOUND meter, no sell templates. Run `31325780684` succeeded for `74f0218` and the served `index.html` references the same asset hash as the local build of that commit, so the deploy is confirmed end to end rather than assumed. Recorded because a stale deploy silently invalidated a round of first-contact feedback: onboarding cannot be judged against a build that does not contain it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-10 | **Added OP-27…OP-31 from owner play, and answered one of OP-22's design questions.** Two are defects confirmed and measured in the browser: **OP-27** (High) — `TerminalPanel`'s scroll-to-bottom effect depends on the snapshot's `terminal` array, which is rebuilt on every revision, so at the 10 Hz timestep the terminal is forced to its newest line ten times a second and cannot be scrolled back at all (verified: one tick with no new line leaves 60 → 60 lines but a fresh array identity); **OP-31** (High) — `.terminal-column` has no scroller while `.terminal-screen` is `overflow: hidden`, so expanded panels are clipped where the page cannot scroll to them (87 px of the execution log lost at 1280 × 720) and the terminal itself renders 0 px tall while holding 61 lines. **OP-28** (Medium) quantifies an unlock-cadence complaint on a cold start: channels are revealed 2.2–4× ahead of affordability, because reveal is keyed to lifetime requests and affordability to capital at 0.25 CR/request — the daemon is listed 16 s before the first install is affordable at all. **OP-29** (Medium) asks for credit flow over time so two algorithms can be compared across an edit, which no existing row covers, and carries a Paradox-style hover-detail proposal. **OP-30** (Low) records an idea for a post-M8 milestone of replay modifiers. A sixth finding — exhausted install channels grouped separately and collapsed by default — is a note inside **OP-22**, which had already posed that exact question; it settles it as "collapse, do not delete", preserving the record of what was bought.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-10 | **Routing for the five new items, recorded because M7.5's scope rule requires it.** OP-27 → **WP3**, OP-31 → **WP5**: both are defect fixes rather than new features, both land in packages that already own the surface, and neither displaces a row. OP-28 → **M8 task 5** (the fix is the balance ladder, which has to move together), cross-flagged to WP6 since a first-time tester meets it inside ninety seconds. OP-29 → the **post-prototype group** the plan already names (OP-9, OP-17, OP-21's attribution half), since per-deployment flow attribution is the plumbing all four need. OP-30 → **candidate M9**, needing a GDD amendment first, and also listed in the Backlog. The cut line gains an exception: if WP5 is dropped, OP-31 comes out of it and is done anyway. Docs-only change; no code touched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-10 | **M7.5 WP3 complete — readouts tell the truth (OP-19, OP-21 display half and OP-27 closed; OP-32 raised).** COMPUTE, ENERGY and CORE TEMP each carry a signed per-second rate on their meter label line, revealed with the rest of the system readouts; the conditional POWER DRAW row is gone, so an oscillating reserve can no longer resize the side column — measured with the reserve at 0.13 of 400, the sign flipped on **199 of 200 consecutive ticks** against **one** panel height (241.51 px) and **one** top for the panel below it (321.36 px). The rates were made honest before they were shown: script fuel is subtracted from the compute and energy rates (readout `-1.32/S` against a pool moving `-1.350/S`), and the core rate is now measured across the whole step rather than from after its heat had landed — it could previously only report cooling. Measured across one thermal engagement: `+0.18°C/S` in the alert colour at 86.0 °C, then `-0.63`, `-2.87` and `-6.64°C/S` at 0.5 s, 2 s and 4 s after the coolant opened. New pure module `/core/rates.ts` holds the trailing window (`BALANCE.readouts.rateWindowSec` = 2 s), lives in the engine closure beside `compiled`, and is never persisted. The system terminal now follows the newest line only for a reader already at it, keyed on the newest line's `id` rather than on an array `getSnapshot()` rebuilds every revision; a reader who scrolls back holds position across **60 consecutive revisions** and gets a floating `N NEW LINES BELOW ↓` control. No save-shape change: v7 stands and M7.5’s single v8 migration stays with WP4b. **326 tests (11 new in `rates.test.ts`).** Verified in-browser at 1280 px and 440 px with the live save backed up and restored intact, no console errors. **Next action: WP4a — process surface, decided (OP-25).**                                                                                                                                                                                                |
| 2026-08-10 | **M7.5 WP4a complete — process surface, decided (OP-25 closed).** The decision is **merge**: PROCESS SCHEDULER (M4) and PROCESS PROFILER (M5) are now one **PROCESS TABLE**, one row per process, divided by question rather than by which milestone built it — live state (declaration, last activation, `N RUNS // N FAIL`, TERMINATE) always visible, and accumulated cost plus the GDD §6 report behind a per-row `COST DETAIL` disclosure. The five fields OP-25 counted as duplicated are each rendered once. "Keep two and strip each" was rejected on WP4b's account: OP-12/13/14 all act on a process, so two panels would have the player renaming a process in one and reading its counters in the other. The engine publishes `ProcessView.profileKey` so the scheduler view and the profile are joined on a value it owns; `TelemetryView` is untouched and **no save version moves**. The cost half stays gated on the instrumentation unlock, so the 480→620 window renders live state only (205.1 px against 486.2 px instrumented, zero disclosures); both grant lines were rewritten to name the panel that now exists. Measured at 1280 px (285 px panel, 642.3 px folded, 876.2 px with the preempted row open) and 440 px (411.2 px wide, 936.5 px with all four rows open, no horizontal overflow), with a row's expansion surviving 50 snapshot revisions at 10 Hz and TERMINATE clearing both halves in one frame. 329 tests (3 new). No GDD amendment: the debugging tools §6 lists are unchanged, only the panel they are read in.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-10 | **M7.5 WP4b complete — process lifecycle (OP-12, OP-13 and OP-14 closed).** The three controls WP4a's merge was decided for now sit on the process row: **DESIGNATE**, **HOLD/RESUME** and **REVISE**, with TERMINATE still alone below them because it is the destructive one. One decision the rows did not anticipate: `PROC-nn` stays the **identifier** and the player's name is a **designation** beside it, because the execution log writes the process name at activation time and a rename that replaced it would split a process's log between two names — the join key OP-12 exists to defend. Input is put in the system voice rather than refused (`queue drain!!` → `QUEUE DRAIN`, cut to 20 characters), so GDD §33.3 holds whatever is typed. **HOLD** keeps the slot, the RAM and every counter — measured at 10 activations, still 10 after 30 s while a neighbour ran 5 → 20 — and resume re-arms rather than resumes: `nextDueTick` back to now, `lastCondition` cleared, so a `when` guard whose condition stood true throughout (a queue at 139) fires the crossing it is owed instead of never firing again. **REVISE** pulls the source back into the editor (a new `EDITOR_SOURCE_SET` event, because CodeMirror owns its document), asks `OVERWRITE BUFFER?` before discarding unsaved work, and turns DEPLOY into `REDEPLOY PROC-nn` — one action that replaces in place, keeping the id, ordinal and designation, resetting the counters and saying so, and admitted at 4/4 slots where a plain DEPLOY of the same source is refused. Save **v8**, M7.5's single migration as planned in TDD §8: `label`, `paused`, `revisingId`, all defaulting to the situation an existing save was already in. 343 tests (14 new). Verified at 1280 px and 440 px with no horizontal overflow and no console errors; reaching the panel needed an artificially tall viewport, which is OP-31 and already WP5's.                                                                                                                           |
| 2026-08-10 | **M7.5 WP5 complete — side column navigation (OP-22, OP-24 and OP-31 closed).** Three separate causes, fixed separately. **OP-31** (High): `.terminal-column` had no scroller of its own inside a locked-height desktop shell, so expanded panels went where nothing could reach them and the whole squeeze landed on the terminal — measured pre-fix at 1280 × 720 as a `.terminal-output` **27.5 px tall holding 50 lines** with a column scroll range of **0**. It now carries `overflow-y: auto` like the side column, and an expanded `.terminal-panel` carries a `min-height: 10rem` floor: the terminal holds 160 px, the output 103 px, and the execution log 75.5 px below the fold is reached by scrolling the column 76 px. **OP-22** (Medium): install channels are sectioned on a new `category` field in `/content/upgrades.ts` (`THROUGHPUT`, `CAPACITY`, `PROCESS EXECUTION`, `POWER AND COOLING`, declared with their order in `UPGRADE_CATEGORIES` and published on `UpgradeView`), and exhausted channels collapse into `CHANNELS EXHAUSTED // n`, folded by default and one line each — 1368.2 px of list becomes 1089.8 px, with no dead `CHANNEL EXHAUSTED` button left anywhere. The shared collapse store now records the ids whose state _differs from that panel's default_, which is what makes a default-collapsed group's opened state representable; every existing panel behaves exactly as before. **OP-24** (Low): the side column takes a share of the extra width (`clamp(300px, 26%, 420px)`) and the shell stops growing at 1560 px and centres — at 1920 the terminal column goes 1556 → 1102.9 px, the side column 300 → 393.1 px, the nine live install descriptions 15 rendered lines → 11, and the market chart 251 → 344.1 px. Verified in-browser at 440, 800, 1280 and 1920 px with no horizontal overflow and no console errors; 348 tests (5 new in `upgrades.test.ts`, pinning that the categories partition the catalogue). No save version moves and no GDD amendment — see TDD §9 and its Decision Log. |
